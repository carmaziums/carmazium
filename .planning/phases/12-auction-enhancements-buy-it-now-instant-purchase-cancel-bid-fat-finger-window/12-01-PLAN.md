---
phase: 12-auction-enhancements-buy-it-now-instant-purchase-cancel-bid-fat-finger-window
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/auctions/auctions.service.ts
  - backend/src/auctions/auctions.controller.ts
  - backend/src/auctions/dto/create-auction.dto.ts
  - backend/src/bids/bids.service.ts
  - backend/src/auctions/auction.gateway.ts
  - backend/src/auctions/auctions.service.spec.ts
autonomous: true
requirements:
  - BIN-01
  - BIN-02
  - BIN-03
  - BIN-04

must_haves:
  truths:
    - "Prisma Auction model has buyItNowPrice Decimal?, buyItNowPendingBuyerId String?, buyItNowPendingAt DateTime? columns"
    - "POST /auctions/:id/bin-trigger sets pending fields; rejects if auction not ACTIVE, BIN not set, or reserve already met"
    - "POST /auctions/:id/bin-confirm ends auction (ENDED status, Sale record, chat thread, notifications); rejects if no pending BIN"
    - "POST /auctions/:id/bin-decline clears pending fields, notifies buyer, auction resumes"
    - "BIN pending auto-cancels in bids.service when new bid amount >= buyItNowPrice"
    - "Lazy BIN expiry: findOne returns cleared pending fields when buyItNowPendingAt + 24h has elapsed"
    - "auctions.service.spec.ts covers all BIN lifecycle branches"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "buyItNowPrice, buyItNowPendingBuyerId, buyItNowPendingAt on Auction"
      contains: "buyItNowPrice"
    - path: "backend/src/auctions/auctions.service.ts"
      provides: "triggerBuyItNow(), confirmBuyItNow(), declineBuyItNow(), endAuctionWithWinner() helper"
      exports: ["triggerBuyItNow", "confirmBuyItNow", "declineBuyItNow"]
    - path: "backend/src/auctions/auctions.controller.ts"
      provides: "POST :id/bin-trigger, POST :id/bin-confirm, POST :id/bin-decline routes"
    - path: "backend/src/auctions/auctions.service.spec.ts"
      provides: "BIN lifecycle unit tests"
      contains: "triggerBuyItNow"
    - path: "backend/src/auctions/auction.gateway.ts"
      provides: "broadcastBinPending() WebSocket event"
  key_links:
    - from: "auctions.controller.ts POST :id/bin-trigger"
      to: "auctions.service.ts triggerBuyItNow()"
      via: "DI injection"
      pattern: "triggerBuyItNow"
    - from: "auctions.service.ts confirmBuyItNow()"
      to: "endAuctionWithWinner() private helper"
      via: "method call"
      pattern: "endAuctionWithWinner"
    - from: "bids.service.ts create()"
      to: "prisma.auction.update BIN pending clear"
      via: "bid amount >= buyItNowPrice check"
      pattern: "buyItNowPendingBuyerId.*null"
---

<objective>
Add Buy It Now backend — DB schema migration, BIN service lifecycle (trigger/confirm/decline), auto-cancel on bid >= BIN price, lazy 24h expiry, and full unit test coverage.

Purpose: Establishes the data layer and all backend logic for BIN. Plan 03 (frontend) depends on these endpoints being live.
Output: Migrated schema, 3 new auction endpoints, endAuctionWithWinner() shared helper, broadcastBinPending() WebSocket event, passing spec file.
</objective>

<execution_context>
@C:/Users/Airaf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Airaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-auction-enhancements-buy-it-now-instant-purchase-cancel-bid-fat-finger-window/12-CONTEXT.md
@.planning/phases/12-auction-enhancements-buy-it-now-instant-purchase-cancel-bid-fat-finger-window/12-RESEARCH.md
@.planning/phases/12-auction-enhancements-buy-it-now-instant-purchase-cancel-bid-fat-finger-window/12-VALIDATION.md

<interfaces>
<!-- Key patterns confirmed from direct codebase inspection (see 12-RESEARCH.md Sources). -->

From backend/prisma/schema.prisma (lines 700-754):
```prisma
model Auction {
  // existing fields include:
  reservePrice           Decimal          @db.Decimal(12, 2)
  winningBidAmount       Decimal?         @db.Decimal(12, 2)
  // ADD after winningBidAmount:
  buyItNowPrice          Decimal?         @db.Decimal(12, 2)
  buyItNowPendingBuyerId String?
  buyItNowPendingAt      DateTime?
}
model Bid {
  // existing fields include:
  deletedAt DateTime?
  // ADD after updatedAt:
  cancelledAt DateTime?
}
```

From backend/src/auctions/auctions.service.ts (confirmed patterns):
- acceptBid() at line ~314: runs a $transaction that creates Sale, updates Listing to SOLD, upserts SellerProfile, broadcasts auction:ended, calls notifyAuctionEnd()
- cancel() at line ~105: pattern for SCHEDULED auction cancellation
- findOne() / findBySlug(): the read methods where lazy BIN expiry check must be inserted

From backend/src/auctions/auction.gateway.ts (line 18):
- BidBroadcastPayload carries bidderId — confirmed
- broadcastBid() pattern: this.server.to(`auction:${auctionId}`).emit('bid:new', payload)
- New method needed: broadcastBinPending(auctionId, buyerId): emit 'bin:pending' event

From backend/src/auctions/auctions.controller.ts:
- PATCH :id/cancel at line 105 (seller cancel for SCHEDULED auctions — existing)
- Pattern: @Post(':id/bin-trigger') @UseGuards(SessionAuthGuard) @ApiBearerAuth()

From backend/src/auctions/dto/create-auction.dto.ts:
- Existing fields: startingBid, reservePrice, scheduledStartAt, duration
- ADD: @IsOptional() @IsNumber() buyItNowPrice?: number

Notification types available in NotificationType enum:
- AUCTION_ENDED — use for seller BIN notification
- AUCTION_WON — use for buyer BIN-accepted notification
(DO NOT add new enum values — avoids breaking migration)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB migration + auctions.service.spec.ts Wave 0 test scaffold</name>
  <files>backend/prisma/schema.prisma, backend/src/auctions/auctions.service.spec.ts</files>
  <behavior>
    - Test: triggerBuyItNow throws BadRequestException when auction.status !== 'ACTIVE'
    - Test: triggerBuyItNow throws BadRequestException when auction.buyItNowPrice is null
    - Test: triggerBuyItNow throws BadRequestException when top bid amount >= reservePrice
    - Test: triggerBuyItNow updates buyItNowPendingBuyerId + buyItNowPendingAt and calls notificationsService.create
    - Test: confirmBuyItNow throws BadRequestException when buyItNowPendingBuyerId is null
    - Test: confirmBuyItNow calls endAuctionWithWinner() and creates Sale record (verify transaction)
    - Test: declineBuyItNow clears pending fields and notifies buyer
    - Test: findOne with expired buyItNowPendingAt (> 24h ago) returns auction with cleared pending fields
    - Test: bids.service create() with amount >= buyItNowPrice AND pending BIN clears pending fields
  </behavior>
  <action>
1. Edit backend/prisma/schema.prisma — add to model Auction after winningBidAmount line:
   ```
   buyItNowPrice          Decimal?  @db.Decimal(12, 2)
   buyItNowPendingBuyerId String?
   buyItNowPendingAt      DateTime?
   ```
   Add to model Bid after updatedAt:
   ```
   cancelledAt DateTime?
   ```
   (cancelledAt is required here too — Plan 02 uses it; both fields go in the same migration)

2. Run migration from the backend directory:
   ```
   cd /d/carmazium/backend && npx prisma migrate dev --name add_bin_and_bid_cancel
   ```

3. Create backend/src/auctions/auctions.service.spec.ts with the 9 test cases listed in behavior above. Use the standard NestJS unit test pattern: TestingModule, mock PrismaService, mock NotificationsService, mock AuctionGateway. All mocks return minimal shaped objects. Write the describe blocks and test expectations BEFORE implementing the service methods. Run `npx jest auctions.service.spec --no-coverage` from backend dir — tests MUST FAIL RED (methods don't exist yet).

4. Commit: `test(12-01): add auctions.service.spec.ts BIN lifecycle tests (RED)`
  </action>
  <verify>
    <automated>cd /d/carmazium/backend && npx jest auctions.service.spec --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>schema.prisma has all 4 new nullable fields; migration applied without error; auctions.service.spec.ts has 9 test cases that fail RED (no implementation yet)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BIN service methods + controller endpoints + gateway event</name>
  <files>backend/src/auctions/auctions.service.ts, backend/src/auctions/auctions.controller.ts, backend/src/auctions/auction.gateway.ts, backend/src/auctions/dto/create-auction.dto.ts, backend/src/bids/bids.service.ts</files>
  <behavior>
    - GREEN: all 9 tests from Task 1 pass
    - endAuctionWithWinner(auctionId, winnerId, amount, sellerId) private helper encapsulates the $transaction (update Auction ENDED + winnerId, update Listing SOLD, create Sale, upsert SellerProfile, broadcast auction:ended, call notifyAuctionEnd)
    - acceptBid() and closeAuction() both call endAuctionWithWinner() — no duplication
    - triggerBuyItNow(): validates ACTIVE status, BIN price set, reserve not already met by top bid; updates pending fields; notifies seller via notificationsService.create (type AUCTION_ENDED); calls gateway.broadcastBinPending()
    - confirmBuyItNow(): validates pending BIN exists; calls endAuctionWithWinner(); clears pending fields as part of the $transaction
    - declineBuyItNow(): clears pending fields; notifies buyer via notificationsService.create (type AUCTION_WON closest)
    - Private clearExpiredBin(auction): called at top of findOne() and findBySlug(); fire-and-forget prisma.auction.update if 24h elapsed
    - bids.service.ts create(): after saving bid, if auction.buyItNowPrice exists AND buyItNowPendingBuyerId AND new bid amount >= buyItNowPrice → fire-and-forget clear pending fields + notify pending buyer
    - Controller: POST :id/bin-trigger, POST :id/bin-confirm, POST :id/bin-decline — all behind SessionAuthGuard, @ApiBearerAuth(), proper @ApiOperation descriptions
    - create-auction.dto.ts: add @IsOptional() @IsNumber() @ApiPropertyOptional() buyItNowPrice?: number
    - auction.gateway.ts: broadcastBinPending(auctionId: string, buyerId: string) emits 'bin:pending' event to room
  </behavior>
  <action>
1. In auctions.service.ts: extract shared endAuctionWithWinner() private method from existing acceptBid() $transaction block (lines 314-343). Wire acceptBid() and closeAuction() to call it. This ensures confirmBuyItNow() reuses the exact same winner-close logic including Sale record creation, sellerProfile upsert, notification broadcast — no duplication (per Pitfall 5 in RESEARCH.md).

2. Add triggerBuyItNow(auctionId: string, buyerId: string): use the skeleton from RESEARCH.md Pattern 1. Key checks:
   - auction.status !== 'ACTIVE' → BadRequestException
   - !auction.buyItNowPrice → BadRequestException
   - top bid amount (from listing.bids[0].amount, ordered desc) >= auction.reservePrice → BadRequestException ('Reserve is met — BIN no longer available')
   - Allow re-trigger by same buyer (replace existing pending)
   - prisma.auction.update sets pendingBuyerId + pendingAt
   - notificationsService.create for seller (type: 'AUCTION_ENDED', link: /auctions/live/:id)
   - this.auctionGateway.broadcastBinPending(auctionId, buyerId)

3. Add confirmBuyItNow(auctionId: string, sellerId: string): check pending BIN exists (buyItNowPendingBuyerId not null); call endAuctionWithWinner(auctionId, pendingBuyerId, auction.buyItNowPrice, sellerId); clear pending fields within the same $transaction.

4. Add declineBuyItNow(auctionId: string, sellerId: string): validate caller is listing.sellerId; clear pending fields via prisma.auction.update; notificationsService.create to notify buyer (pendingBuyerId) that BIN was declined.

5. Add clearExpiredBin(auction): see RESEARCH.md Pattern 1 exact code. Call at start of findOne() and findBySlug() return paths.

6. In bids.service.ts create(): after bid.create and before broadcast, add BIN auto-cancel check per RESEARCH.md Pattern 2.

7. In auction.gateway.ts: add broadcastBinPending(auctionId, buyerId) method following broadcastBid() pattern.

8. In auctions.controller.ts: add three routes:
   ```typescript
   @Post(':id/bin-trigger')
   @UseGuards(SessionAuthGuard)
   @ApiBearerAuth()
   @ApiOperation({ summary: 'Buyer triggers a Buy It Now request' })
   binTrigger(@Param('id') id: string, @Req() req: any) {
     return this.auctionsService.triggerBuyItNow(id, req.user.id);
   }
   // Similar for bin-confirm and bin-decline (seller only — service validates sellerId)
   ```

9. In create-auction.dto.ts: add @IsOptional() @IsNumber() @ApiPropertyOptional({ description: 'Buy It Now price in GBP. Optional. Locked once auction goes ACTIVE.' }) buyItNowPrice?: number;

10. Run tests: `cd /d/carmazium/backend && npx jest auctions.service.spec --no-coverage` — ALL 9 tests must pass GREEN.
11. Run full suite: `cd /d/carmazium/backend && npx jest --no-coverage`
12. Commit: `feat(12-01): BIN trigger/confirm/decline service + controller + gateway broadcast`
  </action>
  <verify>
    <automated>cd /d/carmazium/backend && npx jest auctions.service.spec --no-coverage 2>&1 | tail -20 && npx jest --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>All 9 auctions.service.spec tests GREEN; full backend jest suite passes; three new POST routes respond at runtime (verify with: curl -X POST http://localhost:3001/auctions/fake-id/bin-trigger returns 401 Unauthorized, confirming route is registered)</done>
</task>

</tasks>

<verification>
- `cd /d/carmazium/backend && npx prisma migrate status` shows migration applied
- `cd /d/carmazium/backend && npx jest --no-coverage` all green
- `curl -X POST http://localhost:3001/auctions/test/bin-trigger` → 401 (route exists, guard fires)
- `curl -X POST http://localhost:3001/auctions/test/bin-confirm` → 401
- `curl -X POST http://localhost:3001/auctions/test/bin-decline` → 401
</verification>

<success_criteria>
- schema.prisma has buyItNowPrice, buyItNowPendingBuyerId, buyItNowPendingAt on Auction and cancelledAt on Bid
- Migration applied with zero downtime (all columns nullable)
- auctions.service.ts has triggerBuyItNow(), confirmBuyItNow(), declineBuyItNow(), endAuctionWithWinner(), clearExpiredBin()
- bids.service.ts create() auto-clears BIN pending when bid >= BIN price
- auction.gateway.ts has broadcastBinPending()
- auctions.service.spec.ts has 9 tests all GREEN
- Full backend test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/12-auction-enhancements-buy-it-now-instant-purchase-cancel-bid-fat-finger-window/12-01-SUMMARY.md`
</output>
