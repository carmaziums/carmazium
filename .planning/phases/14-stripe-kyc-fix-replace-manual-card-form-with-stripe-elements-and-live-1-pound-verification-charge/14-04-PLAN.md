---
phase: 14-stripe-kyc-fix-replace-manual-card-form-with-stripe-elements-and-live-1-pound-verification-charge
plan: "04"
type: execute
wave: 3
depends_on:
  - "14-03"
files_modified: []
autonomous: false
requirements:
  - KYC-PAY-06
  - KYC-PAY-07

must_haves:
  truths:
    - "A live card entry works end-to-end: dealer enters card, clicks Submit, £1 charge appears in Stripe dashboard"
    - "A declined card (Stripe test card 4000000000000002) shows inline error and blocks form submission"
    - "Admin sees Stripe Verified badge for the test submission"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the Phase 14 Stripe KYC implementation. Confirm that the full end-to-end flow works with real Stripe test cards before marking the phase complete.

Purpose: Catch UX regressions or integration failures that automated tests cannot cover (Stripe Elements UI behaviour, card decline inline errors, admin panel rendering with live data).
Output: Human approval that the Stripe KYC flow works end-to-end.
</objective>

<execution_context>
@C:/Users/Airaf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Airaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stripe-kyc-fix-replace-manual-card-form-with-stripe-elements-and-live-1-pound-verification-charge/14-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Stripe KYC flow end-to-end</name>
  <files>n/a — checkpoint only, no files modified</files>
  <action>
    Pause for human verification. Claude has no automated action to perform in this task.
    The human verifies the Stripe KYC flow using Stripe test cards as described in how-to-verify below.
  </action>
  <what-built>
    - Step 3 of the dealer KYC form now shows a Stripe CardElement instead of bank transfer instructions
    - Atomic submit: clicking Submit charges the card first, then submits the KYC form
    - Declined cards show inline error and block submission
    - Dealers who have already paid see a green tick ("Verification fee paid") instead of the card form
    - Admin panel shows a Stripe Verified badge (emerald ShieldCheck + PaymentIntent ID) for new-flow records
    - Legacy records still show paymentReference/paymentScreenshot fields in admin
  </what-built>
  <how-to-verify>
    **Pre-requisites:**
    - Ensure `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is set in `.env.local` (Stripe test/live publishable key)
    - Run `npm run dev` in the project root
    - Log in as a dealer account that has NOT yet submitted KYC

    **Test 1 — Card form renders on step 3:**
    1. Navigate to the dealer KYC form (Dashboard → KYC / Verification)
    2. Fill in Steps 1 and 2 with valid data
    3. Reach Step 3 — verify: "Payment Verification" heading, "£1.00 verification fee" label, Stripe card input (NOT bank transfer instructions or paymentReference text field)
    4. Expected: Stripe card input visible with placeholder card number field

    **Test 2 — Declined card blocks submission (KYC-PAY-06):**
    1. On Step 3, enter Stripe test card: `4000 0000 0000 0002` (always declines)
    2. Expiry: any future date, CVC: any 3 digits
    3. Click Submit
    4. Expected: inline error appears below card form ("Your card was declined."), form does NOT advance, KYC NOT submitted

    **Test 3 — Successful charge submits KYC (happy path):**
    1. On Step 3, enter Stripe test card: `4242 4242 4242 4242` (always succeeds)
    2. Expiry: `12/30`, CVC: `123`, postal code: `12345`
    3. Click Submit
    4. Expected: form submits, success message appears ("KYC documents submitted successfully!")
    5. Check Stripe dashboard → Payments — verify a £1.00 GBP payment with description "Dealer KYC verification fee — Carmazium (non-refundable)"

    **Test 4 — Already-paid state (KYC-PAY-07):**
    1. Log in again as the SAME dealer account that just submitted
    2. Navigate back to the KYC form
    3. Step 3 should show: green checkmark + "Verification fee paid · £1 charged on [date]" — NO card form
    4. Expected: no CardElement visible; green tick state displayed

    **Test 5 — Admin panel Stripe badge:**
    1. Log in as admin
    2. Navigate to Admin → Dealer Verification
    3. Open the submission from Test 3
    4. In the "Payment Verification" section: emerald green "Stripe Verified · Auto-Approved" badge with PaymentIntent ID and charge date
    5. Expected: ShieldCheck icon, PI id (pi_xxx...), formatted date visible; NO approve/reject buttons for that section
  </how-to-verify>
  <verify>Human confirms all 5 tests pass by typing "approved"</verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any issues found</resume-signal>
  <done>All 5 manual verification steps confirmed: card form renders, decline blocks submission, success submits KYC, already-paid shows green tick, admin shows Stripe badge</done>
</task>

</tasks>

<verification>
All 5 manual verification steps pass as described above.
</verification>

<success_criteria>
- Step 3 shows Stripe card input, not bank transfer instructions
- Declined test card (4000000000000002) shows inline error, blocks submission
- Successful test card (4242 4242 4242 4242) completes KYC submission; £1 visible in Stripe dashboard
- Re-visiting the KYC form as the same dealer shows the already-paid green tick state (no card form)
- Admin panel shows the Stripe Verified badge for the new submission
</success_criteria>

<output>
After completion, create `.planning/phases/14-stripe-kyc-fix-replace-manual-card-form-with-stripe-elements-and-live-1-pound-verification-charge/14-04-SUMMARY.md`
</output>
