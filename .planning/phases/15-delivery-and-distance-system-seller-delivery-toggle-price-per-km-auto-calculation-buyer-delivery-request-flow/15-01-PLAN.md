---
phase: 15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/src/delivery/delivery.service.spec.ts
  - backend/src/delivery/delivery-expiry.service.spec.ts
autonomous: true
requirements:
  - DEL-01
  - DEL-02
  - DEL-03
  - DEL-04
  - DEL-05
  - DEL-06
  - DEL-07
  - DEL-08
  - DEL-09

must_haves:
  truths:
    - "8 unit test stubs for delivery.service compile and are in RED (failing) state"
    - "1 unit test stub for delivery-expiry.service compiles and is in RED state"
    - "npx jest delivery --no-coverage runs in under 15 seconds and reports 9 failing tests"
  artifacts:
    - path: "backend/src/delivery/delivery.service.spec.ts"
      provides: "8 failing unit test stubs covering all service guards"
      exports: []
    - path: "backend/src/delivery/delivery-expiry.service.spec.ts"
      provides: "1 failing unit test stub for 48h expiry cron sweep"
      exports: []
  key_links:
    - from: "backend/src/delivery/delivery.service.spec.ts"
      to: "backend/src/delivery/delivery.service.ts"
      via: "Jest describe + it stubs expecting throw"
      pattern: "expect.*toThrow|rejects"
---

<objective>
Create failing test stubs (RED state) for the Phase 15 delivery system before writing any implementation.

Purpose: Establishes the TDD contract. Wave 1 (plan 02) must make all 9 tests GREEN by implementing the delivery service. Stubs compile immediately and fail with "not implemented" — no accidental passing tests.
Output: 2 spec files with 9 total failing unit test stubs.
</objective>

<execution_context>
@C:/Users/Airaf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Airaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-CONTEXT.md
@.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-RESEARCH.md
@.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-VALIDATION.md
</context>

<interfaces>
<!-- Pattern: backend/src/offers/offers.service.spec.ts — established NestJS Jest test scaffold -->

Established NestJS test mock pattern from offers.service.spec.ts:
```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { PrismaService } from '../prisma/prisma.service';
import { NotificationsService } from '../notifications/notifications.service';
import { EmailService } from '../email/email.service';
import { DeliveryService } from './delivery.service';

const mockPrisma = {
  deliveryRequest: { findFirst: jest.fn(), create: jest.fn(), update: jest.fn(), findMany: jest.fn() },
  offer: { findFirst: jest.fn() },
  listing: { findUnique: jest.fn() },
};
const mockNotifications = { create: jest.fn() };
const mockEmail = { sendDeliveryRequestEmail: jest.fn() };
const mockHttpService = { axiosRef: { get: jest.fn() } };
const mockConfig = { get: jest.fn() };
```

DeliveryRequest schema fields (from RESEARCH.md):
- status: 'PENDING' | 'ACCEPTED' | 'DECLINED' | 'COMPLETED' | 'CANCELLED'
- expiresAt: DateTime (created_at + 48h)
- buyerId, sellerId, listingId, offerId
</interfaces>

<feature>
  <name>DeliveryService + DeliveryExpiryService unit test stubs</name>
  <files>backend/src/delivery/delivery.service.spec.ts, backend/src/delivery/delivery-expiry.service.spec.ts</files>
  <behavior>
    delivery.service.spec.ts (8 stubs — ALL must fail/throw "not implemented"):
    - DEL-01: "createDeliveryRequest rejects if buyer has no offer on listing"
      → expect throws BadRequestException when prisma.offer.findFirst returns null
    - DEL-02: "createDeliveryRequest rejects duplicate active request"
      → expect throws BadRequestException when existing PENDING/ACCEPTED request found for same buyerId+listingId
    - DEL-03: "createDeliveryRequest rejects out-of-radius request"
      → expect throws BadRequestException when distanceMiles > listing.deliveryMaxMiles
    - DEL-04: "createDeliveryRequest stores distance and cost"
      → expect prisma.deliveryRequest.create called with distanceMiles and estimatedCostGbp
    - DEL-05: "acceptDeliveryRequest throws ForbiddenException for non-seller"
      → expect throws ForbiddenException when caller userId !== request.sellerId
    - DEL-06: "declineDeliveryRequest sends buyer notification"
      → expect notificationsService.create called with userId = request.buyerId
    - DEL-07: "cancelDeliveryRequest rejects non-PENDING status"
      → expect throws BadRequestException when request.status !== 'PENDING'
    - DEL-08: "completeDeliveryRequest rejects non-ACCEPTED status"
      → expect throws BadRequestException when request.status !== 'ACCEPTED'

    delivery-expiry.service.spec.ts (1 stub):
    - DEL-09: "handleDeliveryExpiry cancels PENDING requests past expiresAt"
      → expect prisma.deliveryRequest.findMany called with status PENDING and expiresAt lte now
  </behavior>
  <implementation>
    CRITICAL: Do NOT create delivery.service.ts or delivery-expiry.service.ts yet.
    The spec files must mock the service classes with jest.fn() stubs that throw new Error('not implemented') so tests fail RED immediately.

    Create the directory: backend/src/delivery/ (no other files).

    delivery.service.spec.ts pattern:
    - Use describe('DeliveryService') with nested describe per method
    - Each it() calls a jest.fn() stub and expects it to throw/reject
    - Provider setup uses jest.fn() for the service itself:
      deliveryService = { createDeliveryRequest: jest.fn().mockRejectedValue(new Error('not implemented')), ... }
    - This ensures tests fail RED while compilation succeeds

    Alternative cleaner approach (preferred): Create delivery.service.ts as a SKELETON with the service class and method signatures throwing `throw new Error('not implemented')`. This allows the spec to import and call real methods, which will throw, satisfying RED state. The skeleton must NOT contain any real logic.

    Skeleton delivery.service.ts exports:
    export class DeliveryService {
      async createDeliveryRequest(dto, userId): Promise<any> { throw new Error('not implemented'); }
      async acceptDeliveryRequest(id, userId): Promise<any> { throw new Error('not implemented'); }
      async declineDeliveryRequest(id, userId): Promise<any> { throw new Error('not implemented'); }
      async cancelDeliveryRequest(id, userId): Promise<any> { throw new Error('not implemented'); }
      async completeDeliveryRequest(id, userId): Promise<any> { throw new Error('not implemented'); }
    }

    Similarly, delivery-expiry.service.ts skeleton:
    export class DeliveryExpiryService {
      async handleDeliveryExpiry(): Promise<void> { throw new Error('not implemented'); }
    }

    ALSO REQUIRED before stubs can compile — add DeliveryStatus enum to schema.prisma. The spec needs to reference status values. Add as string literals in tests, NOT importing from @prisma/client (avoids Prisma generate requirement at this stage).

    Run command to verify RED state: cd backend && npx jest delivery --no-coverage
  </implementation>
</feature>

<verification>
```
cd backend && npx jest delivery --no-coverage
```
Expected: 9 tests fail (RED). Zero tests pass. Exit code 1. All stubs compile without TypeScript errors.

```
cd backend && npx tsc --noEmit
```
Expected: Zero new errors from the delivery stub files.
</verification>

<success_criteria>
- `backend/src/delivery/` directory exists with spec files and skeleton service files
- `npx jest delivery --no-coverage` reports 9 FAILING tests — not 0, not passing
- No TypeScript compilation errors introduced
- No implementation logic exists in delivery.service.ts (only `throw new Error('not implemented')`)
</success_criteria>

<output>
After completion, create `.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-01-SUMMARY.md`
</output>
