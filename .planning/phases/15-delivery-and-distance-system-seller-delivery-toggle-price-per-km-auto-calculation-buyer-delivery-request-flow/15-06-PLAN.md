---
phase: 15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow
plan: "06"
type: execute
wave: 4
depends_on:
  - "15-05"
files_modified: []
autonomous: false
requirements:
  - DEL-10
  - DEL-11
  - DEL-12
  - DEL-13
  - DEL-14
  - DEL-15

must_haves:
  truths:
    - "Seller can create a listing with delivery enabled, price-per-mile, and optional radius"
    - "Delivery-enabled listing shows truck/Delivery badge on CarCard in search results"
    - "Search delivery filter narrows results to delivery-enabled listings"
    - "Buyer can submit a delivery request from their offer thread with live cost preview"
    - "Seller receives in-app notification on new delivery request"
    - "Buyer receives in-app notification on accept/decline"
    - "Seller can accept/decline delivery request from their offers dashboard"
    - "Buyer can cancel a pending delivery request"
    - "Buyer can mark delivery as complete"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint — confirm the complete delivery system works end-to-end in the browser across all required flows.

Purpose: No code is written in this plan. The executor runs the checklist below, reports issues, and approves or requests fixes.
Output: Phase 15 approved or issues documented for gap closure.
</objective>

<execution_context>
@C:/Users/Airaf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Airaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-CONTEXT.md
@.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete delivery and distance system:
    - Plan 01: TDD stubs (9 RED → GREEN in plan 02)
    - Plan 02: Backend delivery module — 7 REST endpoints, 48h expiry cron, Google Maps integration, delivery filter on /listings
    - Plan 03: Seller wizard delivery section (toggle + price-per-mile + radius), deliveryApi.ts, listingApi.ts extensions
    - Plan 04: Buyer offers page inline delivery request form with live cost preview, seller offers page accept/decline, /api/delivery-distance route
    - Plan 05: CarCard delivery badge, search page delivery filter, listing detail sidebar delivery section
  </what-built>
  <how-to-verify>
    Run the full test suite first:
    ```bash
    cd backend && npx jest --no-coverage
    ```
    Expected: All 9 delivery tests GREEN, full suite passes.

    Then verify each flow manually:

    **Flow 1 — Seller creates delivery-enabled listing (DEL-10)**
    1. Log in as seller
    2. Go to /dashboard/seller/add-listing
    3. In Step 1, scroll to "Delivery Options"
    4. Check the "Offer delivery for this listing" checkbox
    5. Enter price per mile: 0.50
    6. Enter max delivery radius: 100
    7. Complete and publish the listing
    8. Expected: listing in DB has deliveryAvailable=true, deliveryPricePerMile=0.50, deliveryMaxMiles=100

    **Flow 2 — Delivery badge in search (DEL-12)**
    1. Go to /search
    2. Enable "Delivery available" filter toggle
    3. Expected: only delivery-enabled listings appear; active filter tag shows "Delivery available"
    4. Find the listing from Flow 1 in results — confirm emerald truck/Delivery chip is visible on CarCard

    **Flow 3 — Live cost preview (DEL-11)**
    1. Log in as a buyer who has an existing offer on the delivery-enabled listing
    2. Go to /dashboard/buyer/offers
    3. Find the offer for the delivery-enabled listing
    4. Click "Add delivery request" (should appear below the offer card)
    5. Fill in street and city; enter a UK postcode (e.g. "M1 1AE")
    6. Expected: "Estimated delivery cost: £X" appears within 1-2 seconds of typing postcode
    7. If seller has deliveryMaxMiles set and buyer postcode is outside radius: error message appears

    **Flow 4 — Delivery request submission + seller notification (DEL-11, DEL-13)**
    1. Submit the delivery request form (all fields filled)
    2. Expected: amber "Delivery Requested · Awaiting confirmation" badge replaces the CTA
    3. Log in as seller → check notification bell
    4. Expected: notification "A buyer has requested delivery of your [Car] to [Postcode]. Estimated cost: £X."
    5. Go to /dashboard/seller/offers — confirm "Delivery: PENDING" tag on the relevant offer card

    **Flow 5 — Seller accepts, buyer notified (DEL-14)**
    1. As seller on /dashboard/seller/offers, click "Accept" on the delivery request
    2. Expected: tag updates to "Delivery: ACCEPTED"
    3. Log in as buyer → check notification bell
    4. Expected: notification "Your delivery request for [Car] has been accepted!"
    5. Go to /dashboard/buyer/offers — confirm green "Delivery Confirmed" badge

    **Flow 6 — Buyer marks as complete**
    1. On /dashboard/buyer/offers, click "Mark as received" button (visible when status=ACCEPTED)
    2. Expected: badge changes to green "Delivered"

    **Flow 7 — Seller declines, buyer can re-request**
    1. As seller, create a fresh delivery request (cancel/decline if needed to reset)
    2. Decline the delivery request from /dashboard/seller/offers
    3. Expected: buyer gets "Delivery Declined" notification
    4. On buyer /dashboard/buyer/offers — confirm red "Delivery Declined" badge AND "Add delivery request" CTA re-appears below it

    **Flow 8 — Listing detail sidebar (DEL-12)**
    1. Visit the listing detail page for the delivery-enabled listing (/buy-cars/[slug])
    2. Expected: right sidebar shows emerald "Delivery available" card with price-per-mile info
    3. Visit a listing without delivery — no delivery section should appear

    **Optional — 48h expiry (DEL-15)**
    If you want to verify this without waiting 48h:
    1. Directly update a PENDING delivery request in DB: `UPDATE delivery_requests SET expires_at = NOW() - INTERVAL '1 hour' WHERE status = 'PENDING';`
    2. Wait for 3AM UTC cron OR manually call the expiry handler via a test endpoint
    3. Expected: request changes to CANCELLED, buyer receives expiry notification
  </how-to-verify>
  <resume-signal>
    Type "approved" if all flows pass.
    Type "issues: [description]" to document what failed for gap closure.
  </resume-signal>
</task>

</tasks>

<verification>
All verification is done by the human in the checkpoint above.
</verification>

<success_criteria>
- All 8 manual verification flows pass
- Full backend test suite green (npx jest --no-coverage)
- No TypeScript errors
- Human approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/15-delivery-and-distance-system-seller-delivery-toggle-price-per-km-auto-calculation-buyer-delivery-request-flow/15-06-SUMMARY.md`
</output>
