---
phase: 02-buyer-seller-and-dealer-role-dashboards
plan: "05"
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements: [BUYER-01, BUYER-02, BUYER-03, BUYER-04, SELL-DASH-01, SELL-DASH-02, SELL-DASH-03, SELL-DASH-04, DEALER-01, DEALER-02, DEALER-03]

must_haves:
  truths:
    - "A buyer account sees only buyer dashboard — no seller or dealer tiles"
    - "A seller account sees seller dashboard with offer inbox and earnings — no buyer/dealer tiles"
    - "A dealer account sees dealer dashboard with lead funnel — no buyer/seller tiles"
    - "Offer accept/reject/counter in seller dashboard updates the offer list without manual refresh"
    - "All screens handle no-data state gracefully (no crashes, helpful empty state text)"
  artifacts:
    - path: "mobile/app/dashboard/buyer/index.tsx"
      provides: "Buyer dashboard (visually verified)"
    - path: "mobile/app/dashboard/seller/index.tsx"
      provides: "Seller dashboard with working offer actions (visually verified)"
    - path: "mobile/app/dashboard/dealer/index.tsx"
      provides: "Dealer dashboard with funnel chart (visually verified)"
  key_links:
    - from: "mobile/app/dashboard/index.tsx"
      to: "correct role-specific dashboard"
      via: "user.role from Zustand → Redirect"
      pattern: "role-routing confirmed by manual test"
---

<objective>
Human verification of all three role dashboards on a physical device or simulator. Confirms role routing works, data loads from the live backend, and offer actions update correctly.

Purpose: Automated TypeScript and unit tests cannot verify role isolation with real accounts, live API data shapes, or visual rendering on device. This checkpoint closes that gap.
Output: Confirmation that all 11 requirements work end-to-end.
</objective>

<execution_context>
@C:/Users/Airaf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Airaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-02-SUMMARY.md
@.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-03-SUMMARY.md
@.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all three role dashboards on device</name>
  <action>Human verifies all three role dashboards on device. See how-to-verify instructions below.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && npx jest --passWithNoTests</automated>
  </verify>
  <done>User types "approved" confirming all three dashboards pass the verification checklist below.</done>
  <what-built>
    Three role dashboards:
    - Buyer dashboard: KPI tiles (active bids, active offers, watchlist, won auctions) + bids/offers/history sub-screens
    - Seller dashboard: Overview tiles + offer inbox with Accept/Decline/Counter actions + earnings summary
    - Dealer dashboard: KPI tiles + lead funnel bar chart + conversion rate / avg views stats

    Role-based routing: dashboard/index.tsx redirects synchronously based on user.role from Zustand.
  </what-built>
  <how-to-verify>
    Run the app: `cd mobile && npx expo start --dev-client`

    **Test 1 — Buyer role (BUYER-01 through BUYER-04):**
    1. Log in with a BUYER account
    2. Navigate to the Dashboard tab (or tap "My Dashboard" in Profile)
    3. Confirm you land on the buyer dashboard — 4 KPI tiles visible: Active Bids, Active Offers, Watchlist, Won
    4. Tap "My Bids" link — confirm a list screen opens with bid rows (or "No bids yet" if empty)
    5. Tap back, tap "My Offers" — confirm offers list with status badges
    6. Tap back, tap "Purchase History" — confirm history list
    7. Pull down to refresh on any screen — confirm spinner appears and data reloads

    **Test 2 — Seller role (SELL-DASH-01 through SELL-DASH-04):**
    1. Log in with a SELLER account (or change role via backend if needed)
    2. Navigate to Dashboard — confirm you land on the SELLER dashboard (not buyer or dealer)
    3. Confirm 3 overview tiles: Active Listings, Offers, Enquiries
    4. Scroll to Offer Inbox — if offers exist: confirm buyer name and £ amount show
    5. If offer exists with PENDING status: tap Counter → type an amount → tap Send → confirm offer status updates in list
    6. Scroll to Earnings — confirm section renders (even if "No completed sales yet")
    7. Confirm buyer dashboard tiles (Active Bids, Watchlist) are NOT visible anywhere

    **Test 3 — Dealer role (DEALER-01 through DEALER-03):**
    1. Log in with a DEALER account (may require backend role change — see note below)
    2. Navigate to Dashboard — confirm you land on the DEALER dashboard
    3. Confirm 4 KPI tiles: Active Listings, Auctions, Leads, Sold / Mo
    4. Scroll to Lead Funnel — if leads exist: confirm coloured bar chart renders; if no leads: confirm "No leads yet" text (no crash)
    5. Scroll to Performance — confirm Conversion and Avg Views tiles render (even if showing "—")
    6. Confirm buyer/seller-specific tiles are NOT visible

    **Note on dealer test account:** If no DEALER role account exists in your Supabase, you can temporarily change a user's role via the Supabase Dashboard → Table Editor → users table → set role = 'DEALER'. This is not a blocker for other role tests.

    **Pass criteria:**
    - Each role sees exactly and only their dashboard
    - No white screens, no "Route not found" errors, no RN red screens
    - Data tiles show numbers or 0 (not undefined/NaN)
    - Pull-to-refresh works on at least one screen per role
  </how-to-verify>
  <resume-signal>Type "approved" if all three dashboards pass, or describe any issues found (screen crashes, wrong role shown, missing data, etc.)</resume-signal>
</task>

</tasks>

<verification>
Full test suite must be green before this checkpoint:
`cd mobile && npx tsc --noEmit && npx jest --passWithNoTests`

Human confirmation required after device testing.
</verification>

<success_criteria>
- All three role dashboards render correctly on device with live data
- Role isolation confirmed: no cross-role tile bleed
- Offer inbox actions work (seller dashboard)
- Lead funnel renders without crash (dealer dashboard)
- TypeScript and test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-05-SUMMARY.md`
</output>
