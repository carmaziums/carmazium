---
phase: 02-buyer-seller-and-dealer-role-dashboards
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mobile/app/dashboard/buyer/index.tsx
  - mobile/app/dashboard/buyer/bids.tsx
  - mobile/app/dashboard/buyer/offers.tsx
  - mobile/app/dashboard/buyer/history.tsx
  - mobile/__tests__/dashboard/BuyerDashboard.test.tsx
autonomous: true
requirements: [BUYER-01, BUYER-02, BUYER-03, BUYER-04]

must_haves:
  truths:
    - "Buyer dashboard overview shows 4 KPI tiles: active bids, active offers, watchlist count, won auctions — values from dashboardApi.buyer()"
    - "Buyer can navigate from overview to bids list screen showing each bid's auction status and amount"
    - "Buyer can navigate from overview to offers list screen showing each offer's status badge (pending/accepted/rejected/countered)"
    - "Buyer can navigate from overview to purchase history list screen"
    - "Pull-to-refresh works on all four screens"
    - "TypeScript compiles clean across all four files"
  artifacts:
    - path: "mobile/app/dashboard/buyer/index.tsx"
      provides: "Buyer dashboard overview with 4 KPI tiles and nav links"
      exports: ["default"]
    - path: "mobile/app/dashboard/buyer/bids.tsx"
      provides: "Buyer bids list screen"
      exports: ["default"]
    - path: "mobile/app/dashboard/buyer/offers.tsx"
      provides: "Buyer offers list screen with status badges"
      exports: ["default"]
    - path: "mobile/app/dashboard/buyer/history.tsx"
      provides: "Buyer purchase history list screen"
      exports: ["default"]
  key_links:
    - from: "mobile/app/dashboard/buyer/index.tsx"
      to: "dashboardApi.buyer"
      via: "useQuery(['dashboard', 'buyer'])"
      pattern: "queryKey.*dashboard.*buyer"
    - from: "mobile/app/dashboard/buyer/bids.tsx"
      to: "dashboardApi.buyer"
      via: "useQuery(['dashboard', 'buyer'])"
      pattern: "data\\.bids"
    - from: "mobile/app/dashboard/buyer/offers.tsx"
      to: "dashboardApi.buyer"
      via: "useQuery(['dashboard', 'buyer'])"
      pattern: "data\\.offers"
---

<objective>
Build all four buyer dashboard screens: overview with KPI tiles, bids list, offers list, and purchase history. All screens share a single TanStack Query cache key `['dashboard', 'buyer']` so they stay in sync.

Purpose: Fulfils BUYER-01 through BUYER-04. Buyer lands on their dashboard and can navigate to every section of their activity.
Output: 4 screen files in `mobile/app/dashboard/buyer/`
</objective>

<execution_context>
@C:/Users/Airaf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Airaf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-RESEARCH.md
@.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and codebase. Executor uses these directly. -->

From mobile/src/components/dashboard/KpiTile.tsx (created in Plan 01):
```typescript
export interface KpiTileProps { label: string; value: number | string; accent?: boolean; sub?: string; }
export function KpiTile(props: KpiTileProps): JSX.Element
```

From mobile/src/lib/api/dashboard.ts:
```typescript
export const dashboardApi = {
  buyer: () => apiRequest<any>(ENDPOINTS.DASHBOARD_BUYER),
  // Returns payload shape (field names unknown at plan time — use optional chaining throughout)
  // Expected shape based on research: { activeBids, activeOffers, watchlistCount, wonAuctions, bids[], offers[], history[] }
};
```

TanStack Query v5 pattern (established in Phase 1 / notifications screen):
```typescript
import { useQuery } from '@tanstack/react-query';
const { data, isLoading, isError, refetch, isRefetching } = useQuery({
  queryKey: ['dashboard', 'buyer'],
  queryFn:  dashboardApi.buyer,
});
```

From mobile/src/components/ui — available atoms:
CzScreen, CzButton, CzBadge, CzChip, CzPrice, CzEyebrow

From mobile/src/constants/tokens.ts:
FONT, CZM, SPACING, RADII (all design tokens)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build buyer dashboard overview screen (BUYER-01)</name>
  <files>
    mobile/app/dashboard/buyer/index.tsx,
    mobile/__tests__/dashboard/BuyerDashboard.test.tsx
  </files>
  <behavior>
    - When data loads, 4 KpiTile components render with labels: "Active Bids", "Active Offers", "Watchlist", "Won"
    - KpiTile values come from data?.activeBids, data?.activeOffers, data?.watchlistCount, data?.wonAuctions (default 0 via ?? 0)
    - "Active Bids" and "Active Offers" tiles render with accent=true when their count > 0
    - Screen shows ActivityIndicator when isLoading is true
    - Screen shows "Could not load dashboard" error message when isError is true
    - Pull-to-refresh calls refetch
    - Nav links to /dashboard/buyer/bids, /dashboard/buyer/offers, /dashboard/buyer/history are present
  </behavior>
  <action>
Create `mobile/app/dashboard/buyer/index.tsx`. Use NativeWind className only (no StyleSheet.create). Use CzScreen, CzEyebrow, KpiTile. Use ScrollView with RefreshControl. Place nav links to sub-screens below the KPI grid.

Screen layout:
1. Header: "My Dashboard" eyebrow + "Buyer" heading
2. KPI grid: 2-column grid of 4 KpiTile components (use `flex-row flex-wrap gap-3`)
3. Quick links section: rows for "My Bids", "My Offers", "Purchase History" — each a TouchableOpacity with router.push

KPI tile mapping (use optional chaining and ?? 0 fallback for every field — backend shape is typed `any`):
- activeBids → data?.activeBids ?? 0, accent=true when > 0
- activeOffers → data?.activeOffers ?? 0, accent=true when > 0
- watchlistCount → data?.watchlistCount ?? 0
- wonAuctions → data?.wonAuctions ?? 0

Write the test file `mobile/__tests__/dashboard/BuyerDashboard.test.tsx`. Mock `@tanstack/react-query` — set `useQuery` to return `{ data: { activeBids: 2, activeOffers: 1, watchlistCount: 5, wonAuctions: 0 }, isLoading: false, isError: false, refetch: jest.fn(), isRefetching: false }`. Mock `expo-router` with `jest.mock('expo-router', () => ({ useRouter: () => ({ push: jest.fn() }), useSafeAreaInsets: () => ({ top: 0 }) }))`. Test renders 4 KpiTile labels. Test the loading state. Test the error state.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && npx jest --testPathPattern="BuyerDashboard" --passWithNoTests</automated>
  </verify>
  <done>Screen file exists, TypeScript clean, tests pass for data/loading/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Build buyer bids, offers, and history sub-screens (BUYER-02, BUYER-03, BUYER-04)</name>
  <files>
    mobile/app/dashboard/buyer/bids.tsx,
    mobile/app/dashboard/buyer/offers.tsx,
    mobile/app/dashboard/buyer/history.tsx
  </files>
  <action>
All three screens share the same data source: `useQuery({ queryKey: ['dashboard', 'buyer'], queryFn: dashboardApi.buyer })`. They are read-only list screens — no mutations.

**mobile/app/dashboard/buyer/bids.tsx** (BUYER-02):
- CzScreen with ScrollView + RefreshControl
- Header: "My Bids" with back-arrow TouchableOpacity (router.back())
- List from `data?.bids ?? []`
- Each row shows: vehicle make/model (`bid.listing?.title ?? bid.listingId`), bid amount formatted with CzPrice or `£${bid.amount?.toLocaleString('en-GB')}`, auction status badge using CzBadge
- Status badge color: ACTIVE → blue, ENDED → fg4, WON → emerald, OUTBID → red (use CzBadge kind="standard" with matching bg class)
- Empty state: "No bids yet" centered text when array is empty
- Handle isLoading and isError states identically to overview screen

**mobile/app/dashboard/buyer/offers.tsx** (BUYER-03):
- Same structure as bids screen
- List from `data?.offers ?? []`
- Each row shows: vehicle make/model (`offer.listing?.title ?? offer.listingId`), offer amount `£{offer.amount?.toLocaleString('en-GB')}`, status badge
- Status badge: PENDING → amber, ACCEPTED → emerald, REJECTED → red, COUNTERED → blue
- Empty state: "No offers yet"

**mobile/app/dashboard/buyer/history.tsx** (BUYER-04):
- List from `data?.history ?? data?.purchases ?? data?.orders ?? []` — try all three field names defensively
- Each row shows: vehicle title, purchase price, date formatted `toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })`
- Empty state: "No purchase history yet"

All three screens: NativeWind className only, no StyleSheet. Import useSafeAreaInsets for bottom padding. Use pattern from notifications screen (border-white/5 row separators, text-white/60 secondary text, text-white primary text).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Three screen files exist, each renders a list from dashboard API data, TypeScript compiles with no errors.</done>
</task>

</tasks>

<verification>
`cd mobile && npx tsc --noEmit && npx jest --testPathPattern="BuyerDashboard" --passWithNoTests`

Both must exit 0.
</verification>

<success_criteria>
- dashboard/buyer/index.tsx renders 4 KPI tiles from API data (tests green)
- dashboard/buyer/bids.tsx renders bids list with status badges
- dashboard/buyer/offers.tsx renders offers list with status badges
- dashboard/buyer/history.tsx renders purchase history list
- All screens handle loading, error, and empty states
- Pull-to-refresh available on all screens
- TypeScript: zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-buyer-seller-and-dealer-role-dashboards/02-02-SUMMARY.md`
</output>
