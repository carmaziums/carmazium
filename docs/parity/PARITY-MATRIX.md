# CarMazium — Mobile/Web Parity Matrix

The capability diff between the Next.js web app (`src/`) and the React Native
mobile app (`carmazium app/carmazium app/`), with the NestJS API (`backend/src/`)
as the contract source of truth.

## Rules
- **ID** is `<PASS>-<NNN>` (e.g. `SELL-014`). IDs are permanent. Never renumber, never reuse.
- **Mobile status** is one of:
  - `MISSING` — no mobile implementation at all
  - `PARTIAL` — exists but incomplete vs web
  - `DIVERGENT` — exists but behaves differently from web
  - `PRESENT` — implemented and traced; requires a mobile `file:line` in Notes that proves it
  - `NEEDS_VERIFICATION` — code written this session, awaiting device testing
  - `VERIFIED` — **set by the repo owner only.** Never set by Claude.
- **Gap type**: functionality / visual / data / navigation / state / permissions
- **Effort**: S / M / L
- **Priority**: P0 / P1 / P2 / P3
- Never rewrite a row that was not audited in the current session.

## Passes
| Pass | Scope | Status |
|---|---|---|
| AUTH | onboarding, sign-up/sign-in, session persistence, roles, permission gating, deep links, logout/expiry | Audited 2026-08-31 — 36 rows |
| BUY | browse, search, filters, sort, listing detail, saved items, enquiry/offer, checkout | Audited 2026-08-31 — 32 rows |
| SELL | create listing, media upload, pricing/reserve, publish, edit/manage, close/withdraw | Audited 2026-08-31 — 32 rows |
| AUCTION | bidding, timers, reserve logic, outbid states, auction end, post-auction states | Audited 2026-09-01 — 39 rows |
| DASH | buyer/seller/dealer dashboards, status transitions, empty states, notifications, messaging, profile/settings | Audited 2026-09-01 — 47 rows |
| CROSS | error/loading/empty states, offline, refresh, pagination, toasts, design tokens, navigation structure | Audited 2026-09-01 — 24 rows |

## Matrix

| ID | Pass | Flow | Capability | Web (file:line) | API endpoint | Mobile status | Gap type | Effort | Priority | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| AUTH-001 | AUTH | Onboarding | Pre-auth marketing carousel | *(no web equivalent)* | — | DIVERGENT | functionality | S | P3 | Mobile-only. `screens/onboarding/OnboardingScreen.tsx:36-67` 3 static slides, first route in `navigation/AuthNavigator.tsx:36`. Calls `completeOnboarding()` **pre-auth** (`OnboardingScreen.tsx:156,162`), which writes the same `czm_onboarding_complete` SecureStore flag (`store/authStore.ts:6,89-92`) that gates the post-signup wizard. See OQ-3. |
| AUTH-002 | AUTH | Sign in | Email/password sign-in | `src/app/auth/login/page.tsx:49-83` | Supabase `signInWithPassword` + `POST /auth/supabase-session` | PRESENT | — | — | — | `screens/auth/LoginScreen.tsx:69-77` -> `store/authStore.ts:164-250`. Both handle the unconfirmed-email case: web `login/page.tsx:69-75`, mobile `authStore.ts:173-199`. |
| AUTH-003 | AUTH | Sign in | Google OAuth sign-in | `src/app/auth/login/page.tsx:180-206` | Supabase OAuth | PARTIAL | functionality | M | P1 | Mobile `LoginScreen.tsx:45-67` calls `signInWithOAuth` with `skipBrowserRedirect:true` then `Linking.openURL`. Return leg depends on the manual deep-link handler `App.tsx:193-234`. Not traced end-to-end — no `linking` config on `NavigationContainer` (`App.tsx:254-262`). See AUTH-020. |
| AUTH-004 | AUTH | Sign in | Apple sign-in | `src/app/auth/login/page.tsx:207-212` | — | DIVERGENT | functionality | — | P3 | Dead on **both**. Web button has no onClick (`login/page.tsx:207-212`, `signup/page.tsx:333-338`); mobile shows `Alert.alert('Coming Soon', ...)` (`LoginScreen.tsx:231-238`). Parity already holds — flagged only so it is not mistaken for a mobile gap. |
| AUTH-005 | AUTH | Sign up | Role selection at signup | `src/app/auth/signup/page.tsx:13,60-66,153-157` | `POST /users/sync` (`backend/src/users/users.controller.ts:144-160`) | MISSING | functionality | M | P0 | Web blocks signup until a role is chosen (`signup/page.tsx:62-65`) and offers BUYER / DEALER / FINANCE_PARTNER cards (`signup/page.tsx:153-157`), with SELLER/CONTRACTOR/INSURANCE_PARTNER reachable via `?role=`. Mobile **hardcodes** `const role = 'BUYER'` (`store/authStore.ts:265`, deliberate per comment L258-264). A dealer or finance partner cannot register on mobile. See OQ-1. **DECIDED:** in scope, stays P0. Mobile gains a signup role picker offering **BUYER + DEALER only** — FINANCE_PARTNER deliberately omitted since mobile has no partner dashboard (OQ-1, OQ-5). |
| AUTH-006 | AUTH | Sign up | Email/password sign-up | `src/app/auth/signup/page.tsx:60-151` | Supabase `signUp` + `POST /users/sync` | PRESENT | — | — | — | `screens/auth/SignupScreen.tsx:87-99` -> `store/authStore.ts:252-365`. Both call `/users/sync` (web `signup/page.tsx:93-107`, mobile `authStore.ts:288-297`). |
| AUTH-007 | AUTH | Sign up | Terms and Privacy acceptance | *(none found)* | — | DIVERGENT | functionality | S | P2 | Reverse gap: **mobile is stricter**. Mobile requires an agree-to-terms checkbox before signup is enabled (`SignupScreen.tsx:50,292-305`); grep for `terms` / `agree` in `src/app/auth/signup/page.tsx` returned no matches. Decide which is canonical — see OQ-4. |
| AUTH-008 | AUTH | Sign up | Password minimum length | `src/app/auth/signup/page.tsx` (none, HTML5 only) | `RegisterDto` MinLength(8), `ResetPasswordDto` MinLength(8) | DIVERGENT | data | S | P1 | Four different rules across the codebase: web signup enforces nothing client-side; web reset enforces **6** (`src/app/auth/reset-password/page.tsx:30-33`); mobile signup enforces **8** (`SignupScreen.tsx:50`); mobile reset enforces **8** (`ResetPasswordScreen.tsx:41-45`); backend DTOs enforce **8**. See OQ-2. |
| AUTH-009 | AUTH | Session | Backend user sync | `src/app/auth/signup/page.tsx:93-107`; `src/app/auth/callback/page.tsx:225-268` | `POST /users/sync` | PRESENT | — | — | — | Mobile `store/authStore.ts:288-297`. Both treat failure as non-fatal (web `.catch`, mobile logs only L296). |
| AUTH-010 | AUTH | Session | Supabase to backend session bridge | `src/context/AuthContext.tsx:64-87` | `POST /auth/supabase-session` (`backend/src/auth/auth.controller.ts:132-169`) | PRESENT | — | — | — | Mobile bridges in three places: `authStore.ts:105-108` (init), `205-208` (login), and the signup path. Both send `{token}`. Backend regenerates the session on user switch (`auth.controller.ts:151-155`). |
| AUTH-011 | AUTH | Session | Session persistence across restarts | `src/lib/supabase.ts:23-59` (localStorage) | — | PRESENT | — | — | — | Mobile persists via a hybrid storage adapter in `src/lib/supabase.ts` (SecureStore for tokens, AsyncStorage for the rest) with `autoRefreshToken:true` (`src/lib/supabase.ts:71`). Restored by `initializeAuth` (`authStore.ts:98-162`) called from `App.tsx:184-190`. |
| AUTH-012 | AUTH | Session | Expired-token refresh and retry | `src/lib/apiClient.ts:64-91` | — | PARTIAL | state | M | P1 | Neither client retries a failed request, but web at least redirects (see AUTH-014). Mobile `src/lib/apiClient.ts` has no refresh-and-retry; it relies entirely on Supabase `autoRefreshToken`. If the refresh fails while the app is backgrounded, every call throws `NO_SESSION` (`apiClient.ts:53-55`) with no recovery path. |
| AUTH-013 | AUTH | Session | Global auth-state-change subscription | `src/context/AuthContext.tsx:110-147` | — | MISSING | state | M | P0 | Web subscribes globally and handles `SIGNED_IN` / `TOKEN_REFRESHED` / `SIGNED_OUT` / `PASSWORD_RECOVERY` (`AuthContext.tsx:116-141`). Mobile has **no** `onAuthStateChange` subscription in `store/authStore.ts` — the only subscriber repo-wide is `screens/auth/VerifyEmailScreen.tsx:38-48`, scoped to `SIGNED_IN` and only while that screen is mounted. A remote sign-out or a refresh failure is never observed. |
| AUTH-014 | AUTH | Session | 401 to re-authentication | `src/lib/apiClient.ts:9-20,26-41,119` | — | PARTIAL | state | M | P0 | Web calls `redirectToLogin()` then throws the `AUTH_REDIRECT` sentinel (`apiClient.ts:9-20,119`), so an expired session bounces the user to `/auth/login?redirect=<path>`. Mobile throws `AUTH_REDIRECT` (`src/lib/apiClient.ts:93-95`) but performs **no navigation**; I found no global handler in `App.tsx`, `RootNavigator.tsx`, or `authStore.ts`. Not traced into `ChatContext` — see OQ-6. |
| AUTH-015 | AUTH | Password | Forgot-password request | `src/app/auth/forgot-password/page.tsx:23-42` | Supabase `resetPasswordForEmail` | PRESENT | — | — | — | Mobile `screens/auth/ForgotPasswordScreen.tsx:34-52`, `redirectTo:'carmazium://reset-password'` (L40) vs web `${baseUrl}/auth/callback?redirect_to=/auth/reset-password` (L28-32). Both validate non-empty only. |
| AUTH-016 | AUTH | Password | Set new password from recovery link | `src/app/auth/reset-password/page.tsx:22-90` | Supabase `updateUser` + `POST /auth/supabase-session` | DIVERGENT | data | S | P1 | Min length differs (6 web vs 8 mobile — see AUTH-008). Mobile also force-logs-out after success and routes to Login (`ResetPasswordScreen.tsx:66-69`); web bridges the new session and routes to `/dashboard` (`reset-password/page.tsx:61-82`). Different end states from the same flow. |
| AUTH-017 | AUTH | Verification | Resend verification email | `src/app/auth/onboarding/page.tsx:146-174` | `POST /auth/send-verification` (`backend/src/auth/auth.controller.ts:191-203`) | DIVERGENT | functionality | S | P1 | Web posts to the backend endpoint, which sends via Resend using an admin-generated magiclink (`backend/src/auth/auth.service.ts:387-476`). Mobile instead calls `supabase.auth.resend({type:'signup'})` directly (`VerifyEmailScreen.tsx:50-62`, `PostSignupOnboardingScreen.tsx:200`) — a **different mail path with different templates and expiry**. Cooldowns also differ: 60s web, 60s `VerifyEmailScreen.tsx:27,56`, 30s `PostSignupOnboardingScreen.tsx:204-215`. |
| AUTH-018 | AUTH | Verification | Pending-verification holding screen | `src/app/auth/onboarding/page.tsx:255-333` (step `verify`) | — | DIVERGENT | navigation | S | P2 | Web makes it step 1 of the onboarding wizard and offers "Go to Dashboard anyway" (`onboarding/page.tsx:321-327`), bypassing verification. Mobile gives it a dedicated root-level screen (`RootNavigator.tsx:33-37` -> `VerifyEmailScreen.tsx`) with **no bypass** — only "Back to sign in" (`VerifyEmailScreen.tsx:141`). Mobile is the stricter gate. |
| AUTH-019 | AUTH | Deep links | OAuth and magic-link callback handling | `src/app/auth/callback/page.tsx:37-191` | — | PARTIAL | navigation | L | P0 | Web handles 7 ordered branches: error param, hash recovery, PKCE `?code=` with AbortError rescue, implicit hash tokens, getSession fallback with a 15s race, and a global 15s safety timer (`callback/page.tsx:44-199`). Mobile `App.tsx:193-234` handles only implicit tokens (L207-224) and a PKCE `code` fallback (L225-233) — no error-param branch, no rescue-on-abort, no timeout safety net. |
| AUTH-020 | AUTH | Deep links | React Navigation linking config | n/a (Next.js routing) | — | MISSING | navigation | L | P0 | `NavigationContainer` is mounted with `ref` and `theme` only — **no `linking` prop** (`App.tsx:254-262`; grep for `linking` in `App.tsx` returns only the `expo-linking` import). All deep-link handling is ad hoc in `App.tsx:193-234` and matches Supabase auth tokens exclusively. `app.json:10` sets scheme `carmazium`; Android intentFilters exist (`app.json:46-60`) but there are no iOS `associatedDomains` and no `data.host` / `pathPrefix`, so `https://carmazium.com/...` links do not open the app on either platform. Any non-auth URL is unroutable. |
| AUTH-021 | AUTH | Onboarding | Post-signup wizard structure | `src/app/auth/onboarding/page.tsx:47` (5 states) | `PATCH /users/me` | PARTIAL | functionality | M | P1 | Web: `verify -> name -> location -> preferences -> done`. Mobile: 4 steps `name -> verify -> postcode -> preferences` (`PostSignupOnboardingScreen.tsx:25`), **no `done` step** — `completeOnboarding()` flips `RootNavigator` straight to Main (`PostSignupOnboardingScreen.tsx:322`). Step order also differs (mobile puts name before verification). |
| AUTH-022 | AUTH | Onboarding | Name capture step | `src/app/auth/onboarding/page.tsx:102-120` | `PATCH /users/me` | PRESENT | — | — | — | Mobile `PostSignupOnboardingScreen.tsx:170-184`. Both require first and last non-empty and both are non-skippable. Mobile additionally skips the step entirely if the name is already known (`needsName`, L136-137). |
| AUTH-023 | AUTH | Onboarding | Location capture step | `src/app/auth/onboarding/page.tsx:179-192` | `PATCH /users/me` | DIVERGENT | data | M | P1 | Web requires **both** `location` and `postcode`, non-empty, non-skippable (`onboarding/page.tsx:179-186`), and mirrors to `localStorage.carmazium_user_location` (L191). Mobile collects **postcode only**, validates against `UK_POSTCODE_REGEX` (`PostSignupOnboardingScreen.tsx:27,245-248`), and lets the user **skip** it (L276-280). Since `hasCompletedOnboarding` is derived from `!!profile.location` (`authStore.ts:123-127`), a mobile user who skips may be re-prompted on every launch. Needs device verification. |
| AUTH-024 | AUTH | Onboarding | Preferences step | `src/app/auth/onboarding/page.tsx:211-213` | `PATCH /users/me` | PRESENT | — | — | — | Mobile `PostSignupOnboardingScreen.tsx:296-324`. Both optional; both swallow save failures and advance anyway (web L211-213, mobile L315-317). |
| AUTH-025 | AUTH | Onboarding | Returning-user skip window | `src/app/auth/onboarding/page.tsx:15,93-99` | — | MISSING | state | S | P2 | Web only shows location/preferences if the account was created within `FRESH_SIGNUP_WINDOW_MS` (10 min); otherwise `router.replace('/dashboard')`. Mobile has no equivalent age check — the gate is purely `hasCompletedOnboarding` (`RootNavigator.tsx:40-41`). |
| AUTH-026 | AUTH | Roles | Role-based post-login routing | `src/app/dashboard/page.tsx:25-44` | `GET /users/me` | PARTIAL | permissions | L | P0 | Web routes all 6 non-admin roles to distinct dashboards: BUYER/SELLER to `/dashboard/user`, DEALER to `/dashboard/dealer`, CONTRACTOR to `/dashboard/service`, FINANCE_PARTNER to `/dashboard/finance`, INSURANCE_PARTNER to `/dashboard/insurance`, unknown to `/dashboard/seller`. Mobile collapses the 7-value `UserRole` enum (`backend/prisma/schema.prisma:18-27`) into 3 values `'buyer' / 'seller' / 'dealer'` (`store/authStore.ts:60,68`), mapping everything else to `buyer` (`authStore.ts:117`). |
| AUTH-027 | AUTH | Roles | Admin routing and gating | `src/app/auth/login/page.tsx:38-47`; `src/app/dashboard/admin/page.tsx:24-29` | — | MISSING | permissions | — | P3 | Web sends ADMIN to `/dashboard/admin` on login and guards the route. Mobile has no ADMIN concept — the role mapping at `authStore.ts:117` folds ADMIN into `buyer`. Likely intentional; confirm before scoping. See OQ-5. **DECIDED: OUT OF SCOPE.** ADMIN is not coming to mobile (OQ-5). Ensure a non-supported role gets a clear message rather than silently landing in the buyer UI. |
| AUTH-028 | AUTH | Roles | Dealer verification gating | `src/app/dashboard/dealer/layout.tsx:107-227` | `GET /users/me` then `dealerProfile.isVerified` | PRESENT | — | — | — | Mobile gates 9 dealer screens via `withDealerGate` (`navigation/MainStackNavigator.tsx:66-74`); `DealerKYC` and `DealerOnboarding` are deliberately ungated (`MainStackNavigator.tsx:205-218`). Gate condition `isVerified` OR `isDealerStaff` (`src/components/DealerGate.tsx:48-54`). Matches the web shape; per-screen contents not audited in this pass. |
| AUTH-029 | AUTH | Roles | Partner role dashboards | `src/app/dashboard/page.tsx:33-40` | — | MISSING | permissions | L | P2 | CONTRACTOR, FINANCE_PARTNER and INSURANCE_PARTNER have dedicated web dashboards. Mobile has no route for any of them; such a user is mapped to `buyer` (`authStore.ts:117`) and lands in the buyer UI. `FinanceScreen.tsx` / `ServicesScreen.tsx` exist under `screens/main/` but are consumer-facing, not partner dashboards — not read in this pass. **DECIDED: OUT OF SCOPE.** Partner roles are not coming to mobile (OQ-5). |
| AUTH-030 | AUTH | Invites | Accept dealer-staff invite | `src/app/auth/accept-invite/page.tsx:22-55` | `POST /dealers/invites/accept` | DIVERGENT | navigation | M | P2 | Both call the same endpoint (web L41-44, mobile `screens/main/AcceptInviteScreen.tsx:64-65`). Web auto-accepts from the `?token=` URL param. Mobile **cannot receive the link** — the route is declared `AcceptInvite: undefined` with no params (`MainStackNavigator.tsx:143,320`) and there is no linking config (AUTH-020), so the screen asks the user to paste the link and regexes the token out (`AcceptInviteScreen.tsx:31-37,55`). Self-documented as a deliberate workaround in the file header comment. |
| AUTH-031 | AUTH | Sign in | Partners portal login | `src/app/auth/partners/page.tsx:30-72` | `POST /auth/login` | MISSING | functionality | M | P2 | No mobile equivalent. Note the web page appears **broken independently of mobile**: it posts `{email, supabaseToken}` (`partners/page.tsx:54-60`) but `LoginDto` accepts only `{email, password}` and the global pipe sets `forbidNonWhitelisted:true` (`backend/src/main.ts:116`), so this should 400. See OQ-7 — do not port until resolved. **DECIDED: OUT OF SCOPE.** The web page is dead — broken against its own contract and with zero inbound links. Logged for web deletion (OQ-7). |
| AUTH-032 | AUTH | Session | Sign out | `src/context/AuthContext.tsx:154-168` | `POST /auth/logout` | PRESENT | — | — | — | Mobile `store/authStore.ts:367-393`. Same ordering on both: backend logout first (best-effort), then `supabase.auth.signOut()`, then local state reset. Mobile resets `role` and `accountRole` to `'buyer'` (L383-391). |
| AUTH-033 | AUTH | Account | Delete account | `src/components/dashboard/DeleteAccountSection.tsx:73-97` | `DELETE /users/me` (`backend/src/users/users.controller.ts:231-262`) | MISSING | functionality | M | P1 | Web requires typing `DELETE` to confirm (`DeleteAccountSection.tsx:73,80,97`). Grep for `deleteAccount` / `Delete Account` / `confirmation.*DELETE` across the whole mobile `src/` returned **no matches**. Relevant to app-store policy for account-creating apps. |
| AUTH-034 | AUTH | Session | Redirect-after-login | `src/app/auth/login/page.tsx:26,38-47`; `src/lib/apiClient.ts:9-20` | — | MISSING | navigation | M | P1 | Web preserves the intended destination through `?redirect=` and restores it post-login. Mobile has no equivalent: `RootNavigator.tsx:33-44` swaps whole stacks, so a session expiry drops the user at the Login screen and then at the Main stack root, losing the screen they were on. |
| AUTH-035 | AUTH | Session | Cold-start auth gate | `src/context/AuthContext.tsx:90-107` | — | PARTIAL | state | S | P2 | Mobile gates only on fonts (`App.tsx:242-244`), then mounts `RootNavigator` immediately. `initializeAuth()` runs async (`App.tsx:184-190`) while `isAuthenticated` still holds its initial `false` (`authStore.ts:81-87`), so a signed-in user can see the Login screen flash before restore completes. `authStore.isLoading` exists (L59) but `RootNavigator.tsx:20-44` never reads it. Needs device verification. |
| AUTH-036 | AUTH | Session | Session lifetime | — | `express-session` `maxAge` 7 days (`backend/src/main.ts:78`) | PRESENT | — | — | — | Backend `sid` cookie TTL is 7 days. Both clients rely on the Supabase JWT plus this cookie; no client-side TTL logic on either side. Recorded so the number is on file for the expiry-handling rows (AUTH-012/014). |
| BUY-001 | BUY | Browse | Listing grid + pagination model | `src/app/search/page.tsx:89,383-413,1293-1335` | `GET /listings` (`backend/src/listings/listings.controller.ts:127-150`) | DIVERGENT | functionality | M | P2 | Web is page-numbered with `PAGE_SIZE = 10` (`search/page.tsx:89`) and Prev/Next controls. Mobile is infinite scroll at `limit: 20` with `hasMore = rawItems.length === 20` (`screens/main/SearchScreen.tsx:280-328,354-358`, `onEndReached` L689). Both are legitimate platform conventions — flagged as an intentional divergence to confirm, not a defect. Backend caps nothing: `limit` has `@Min(1)` and **no `@Max`** (`backend/src/listings/dto/listing-filter.dto.ts:209-214`). |
| BUY-002 | BUY | Browse | Featured listings row | `src/app/search/page.tsx:220-222,1172-1215` | `GET /listings/featured` (`listings.controller.ts:156-165`) | PRESENT | — | — | — | Mobile fetches it in the Home rails via `getFeaturedListings()` (`screens/main/HomeScreen.tsx:369-385`). Web pins it above the search grid; mobile shows it only on Home, not above search results. Minor placement difference, same data. |
| BUY-003 | BUY | Search | Text search input behaviour | `src/app/search/page.tsx:489-495,533-544` | `search` param | DIVERGENT | functionality | S | P2 | Reverse gap — **mobile is better**. Web fires only on explicit submit (Enter or button), no debounce (`search/page.tsx:489-495`). Mobile debounces 350ms and searches as you type (`SearchScreen.tsx:374-379`). Web's own compare page *does* debounce at 500ms (`src/app/compare/page.tsx:111-115`), so the behaviour is inconsistent within web itself. Do not "fix" mobile down to submit-only. |
| BUY-004 | BUY | Search | AI natural-language search | `src/app/HomeClient.tsx:59-81,218-232` | `src/lib/aiApi.ts` (internals not read) | PRESENT | — | — | — | Mobile has it in two places: a SearchScreen modal (`SearchScreen.tsx:704-743`, `handleAiSearch` L480-513) and a Home inline bar (`HomeScreen.tsx:529-570`). Mobile maps 12 AI-returned keys onto filter state; a comment at `SearchScreen.tsx:488-491` records that `listingType`/`sellerType`/`vehicleType`/`location`/`ulezCompliant`/`deliveryAvailable` are deliberately not extracted. |
| BUY-005 | BUY | Search | Filter parameter coverage | `src/app/search/page.tsx:95-148,343-380` | `ListingFilterDto` — 38 optional params (`listing-filter.dto.ts`) | PRESENT | — | — | — | Contrary to expectation, mobile is at near-parity. `buildParams` sends 31 filter params (`SearchScreen.tsx:280-328`) against the backend's 38. The 4 it omits are two deprecated aliases (`year`, single `transmission`), one dead param (`includeSold` — declared at dto:249-256 but never destructured in `listings.service.ts:359-374`), and `markedForExport`. Individual filter *values* diverge — see BUY-006/007/008. |
| BUY-006 | BUY | Search | Transmission filter values | `src/app/search/page.tsx:51-57` | `TransmissionType` enum (`backend/prisma/schema.prisma:49-56`) | DIVERGENT | data | S | P0 | **Mobile sends an invalid enum value.** Backend enum is `MANUAL, AUTOMATIC, CVT, SEMI_AUTOMATIC`. Web maps its labels correctly via `TRANS_MAP` (`search/page.tsx:51-57`). Mobile hardcodes `['AUTOMATIC','MANUAL','SEMI_AUTO']` (`SearchScreen.tsx:915`) — `SEMI_AUTO` is not a member of the enum, and mobile omits `CVT` entirely. The `transmissions` DTO field has `@IsArray()` but **no** per-item `@IsEnum` (`listing-filter.dto.ts:92-96`), so the bad value passes validation and reaches Prisma. **Whether that 500s or returns zero rows is unverified — I did not run it.** Either way the Semi-Auto filter is broken on mobile and CVT vehicles are unreachable. |
| BUY-007 | BUY | Search | Make filter source list | `src/app/search/page.tsx:644-659` | `make` param | PARTIAL | data | S | P1 | Web offers a text input with a datalist autocomplete backed by the full `CAR_MAKES` list from `src/lib/carData.ts`. Mobile offers a single-select chip list of **10 hardcoded makes** (`MAKES`, `SearchScreen.tsx:60`). Any make outside those 10 cannot be filtered on mobile. Note both are single-value: a comment at `SearchScreen.tsx:766-771` correctly records that the backend has no `makes[]` array param. |
| BUY-008 | BUY | Search | Condition filter values | `src/app/search/page.tsx:59-68` | `conditions[]`, `VehicleCondition` enum | PARTIAL | data | S | P2 | Web offers 8 values including `CAT_C` and `CAT_D` (`search/page.tsx:59-68`). Mobile offers 6 (`SearchScreen.tsx:112-119`) — `CAT_C` and `CAT_D` are missing, so those write-off categories cannot be filtered on mobile. |
| BUY-009 | BUY | Search | ULEZ filter tri-state | `src/app/search/page.tsx:842-857` | `ulezCompliant` boolean | PARTIAL | data | S | P3 | Web is tri-state — yes / no / either (`search/page.tsx:842-857`). Mobile is a plain boolean toggle sending `true` or omitting (`SearchScreen.tsx` `buildParams`), so "show me only non-compliant vehicles" is not expressible on mobile. |
| BUY-010 | BUY | Search | Sort options | `src/app/search/page.tsx:77-85` | `sortBy` (6 accepted values, `listings.service.ts:486-492`) | PRESENT | — | — | — | Both offer the same 7 labels; mobile `SORT_OPTIONS` at `SearchScreen.tsx:99-107`. Note for both: `newest` is **not** one of the six values the service branches on (`listings.service.ts:486-492`) — it falls through to the default order `[status asc, isFeatured desc, createdAt desc]`, which happens to equal "newest". Harmless today, fragile if the default ever changes. |
| BUY-011 | BUY | Search | Distance / near-me filter | `src/app/search/page.tsx:89,933-967,391-402` | *(no backend param)* | PRESENT | — | — | — | Both apply it client-side after fetch, because the backend has no lat/lng/radius param. Mobile does the same haversine post-filter (`SearchScreen.tsx:336-348`) and its comment at L138-143 explicitly records that this mirrors web's own limitation — neither reaches across pages. Same chip values (10/25/50/100/200 mi) on both. |
| BUY-012 | BUY | Search | Quick filter chips | *(no web equivalent)* | — | DIVERGENT | functionality | — | P3 | Mobile-only convenience row: All, Under £15k, Under £30k, Electric, Hybrid, SUVs, Sedan, 2020+, Manual (`QUICK_FILTERS`, `SearchScreen.tsx:48-58`). Extra capability, not a gap. |
| BUY-013 | BUY | Detail | Vehicle detail — core sections | `src/app/buy-cars/[slug]/VehicleDetailsPageClient.tsx:586-1567` | `GET /listings/:slug` (`listings.controller.ts:304-329`) | PARTIAL | functionality | L | P1 | Web renders 12 ordered sections. Mobile `screens/vehicle/VehicleDetailScreen.tsx` (3570 lines) confirmed present: image gallery (L630-680), HPI (L542,604), finance calculator (L1509+), delivery quote (L265-288), seller info + reviews (L1362-1436), offer CTA (L1801-1810), linked-auction banner (L984-1000), chat (L388-399). **Not traced:** spec sheet, video embeds, damage/condition report, MOT/tax history — located by grep but not read line by line. Sub-rows BUY-014..016 cover the ones I could resolve; the rest need a follow-up read before this row can be closed. |
| BUY-014 | BUY | Detail | Similar vehicles module | *(not found on web either)* | — | PRESENT | — | — | — | Neither app has it. Recorded so it is not later mistaken for a mobile gap. |
| BUY-015 | BUY | Detail | Video embeds | `src/app/buy-cars/[slug]/VehicleDetailsPageClient.tsx:977-1019` | `listing.videoUrls` | NEEDS_VERIFICATION | functionality | S | P2 | Web renders YouTube iframes / external link cards from `listing.videoUrls`. I could **not** confirm presence or absence on mobile — `VehicleDetailScreen.tsx` was not read in full. Do not treat as a gap until read. |
| BUY-016 | BUY | Detail | Condition, damage records and 3D viewer | `src/app/buy-cars/[slug]/VehicleDetailsPageClient.tsx:1111-1195` | `GET /damage/{listingId}` | NEEDS_VERIFICATION | functionality | L | P1 | Web always renders this: computed exterior grade 1-5, `ThreeDVehicleViewer` (dynamic WebGL import behind an error boundary), and per-zone damage list with photo lightbox from `getDamageRecords`. Mobile shows a `GradeChip` on cards, but whether the damage/3D section exists on the detail screen was **not traced**. The WebGL viewer specifically is unlikely to port as-is — flagging as a probable native-adaptation decision, not a straight port. |
| BUY-017 | BUY | Detail | Retail vs auction routing from search | `src/app/search/page.tsx:969-983` (listingType filter) | `listingType` param | DIVERGENT | navigation | S | P0 | Both let the user filter search results to `AUCTION`. On mobile, `handleCardPress` navigates to `'VehicleDetail'` **unconditionally, regardless of listing type** (`SearchScreen.tsx:223-226`) — auction results open in the retail detail screen rather than `LiveAuctionDetailed`. Mobile otherwise routes auctions correctly from Home (`HomeScreen.tsx:418-419,434-438`). I did not read `HorizontalVehicleCard.tsx` in full, so an internal redirect cannot be fully excluded — but `handleCardPress` itself has no type branch. |
| BUY-018 | BUY | Detail | Buy It Now (auction) | `src/app/buy-cars/[slug]/VehicleDetailsPageClient.tsx:1416-1433,427-440` | `triggerBuyItNow` | NEEDS_VERIFICATION | functionality | M | P2 | Web shows a Buy It Now box when `type === 'AUCTION' && auction.status === 'ACTIVE' && buyItNowPrice && !reserveMet`. Mobile equivalent lives in `AuctionDetailScreen.tsx`, which is Pass 4 scope — deferred rather than guessed at here. |
| BUY-019 | BUY | Saved | Add / remove from watchlist | `src/lib/listingApi.ts:656-673` | `POST` / `DELETE /watchlist/:listingId` (`watchlist.controller.ts:58-88`) | PRESENT | — | — | — | Mobile `store/watchlistStore.ts:48-99` — optimistic add/remove with revert on failure, via `src/lib/watchlistApi.ts:43-56`. Mobile additionally swallows the backend's 409 conflict (`watchlistApi.ts:43-52`), which is correct given the endpoint returns 409 when already present (`watchlist.controller.ts:58-72`). |
| BUY-020 | BUY | Saved | Saved-items list + pagination | `src/lib/listingApi.ts:649-654` | `GET /watchlist?page=&limit=` | PARTIAL | data | S | P2 | Backend paginates (`watchlist.controller.ts:33-53`). Mobile `hydrateFromApi()` fetches a **single page of 50** and never paginates (`store/watchlistStore.ts:27-46`); its comment explains this is because the same store drives every heart icon app-wide. A user with more than 50 saved vehicles silently sees a truncated list. |
| BUY-021 | BUY | Saved | Duplicate saved-items screens | n/a | — | DIVERGENT | navigation | S | P2 | Mobile has **two** screens over the same store data. `SavedScreen.tsx` is the live one — a bottom-tab entry (`navigation/TabNavigator.tsx:20,36,52,201`) with grid/list toggle and status badges. `WatchlistScreen.tsx` is registered in the stack (`MainStackNavigator.tsx:55,144,321`) but `grep -rn "navigate('Watchlist'" src/` returns **0 matches** — it is unreachable dead code duplicating `SavedScreen`. Recommend deletion, but that is a call for you to make. |
| BUY-022 | BUY | Offers | Make an offer — minimum amount rule | `src/app/buy-cars/[slug]/VehicleDetailsPageClient.tsx:115-124` | `POST /offers`, 70% floor (`backend/src/offers/offers.service.ts:53-64`) | DIVERGENT | data | S | P0 | **Mobile violates the backend contract.** Backend rejects any offer below `floor(askingPrice * 0.7)`. Web mirrors it exactly: `minAllowedOffer = Math.floor(askingPrice * 0.7)`, prefilled at 90% (`VehicleDetailsPageClient.tsx:115-124`). Mobile instead clamps to an **absolute £15,000 window**: `OFFER_MIN = listing.price - 15000` (`VehicleDetailScreen.tsx:317`). These diverge in both directions — on a £10k car mobile's floor is negative, letting the user submit below 70% and get a generic `Alert` from the server (`VehicleDetailScreen.tsx:348-362`); on a £100k car mobile's floor is £85k, blocking offers the backend would accept down to £70k. |
| BUY-023 | BUY | Offers | Offer payload fields | `src/lib/listingApi.ts:954-966` | `CreateOfferDto` | PARTIAL | data | S | P1 | Web sends `{listingId, amount, amountMin, amountMax, message}`. Mobile sends only `{listingId, amount}` (`VehicleDetailScreen.tsx:352-355`) — no buyer message, and no `amountMax`, which the backend uses as the effective bid for the 70% check and for outbidding comparisons (`offers.service.ts:53-64,66-92`). |
| BUY-024 | BUY | Offers | Counter-offer respond (buyer side) | `src/app/vehicle/[id]/VehicleDetailPageClient.tsx:710-737` (legacy route only) | `PATCH /offers/:id/respond-counter` (`offers.controller.ts:158-168`) | PRESENT | — | — | — | Reverse gap — **mobile is ahead**. Mobile `BuyerOffersScreen.tsx:268-343` implements accept / decline / counter-back, gated on `isBuyerTurn = isCountered && lastCounteredBy === 'SELLER'` (L483) and locked after 5 buyer attempts (L485), correctly mirroring the backend limit (`offers.service.ts:653-655`). On web this exists **only on the legacy `/vehicle/[id]` route** — the canonical `/buy-cars/[slug]` page has no counter-offer UI. |
| BUY-025 | BUY | Offers | Withdraw offer | *(not on canonical web detail page)* | `PATCH /offers/:id/withdraw` (`offers.controller.ts:141-153`) | PRESENT | — | — | — | Mobile `BuyerOffersScreen.tsx:242-266`, PENDING only — matches the backend rule (`offers.service.ts:577-579`). |
| BUY-026 | BUY | Offers | Offer list refresh behaviour | n/a | `GET /offers/my` | PRESENT | — | — | — | Mobile refetches on every screen focus via `useFocusEffect` (`BuyerOffersScreen.tsx:228-238`), not just mount — appropriate for native and better than a mount-only fetch. |
| BUY-027 | BUY | Enquiry | Enquire / message seller | `src/app/buy-cars/[slug]/VehicleDetailsPageClient.tsx:448-482` | `POST /chat/rooms` | PRESENT | — | — | — | Mobile `VehicleDetailScreen.tsx:388-399` and `BuyerOffersScreen.tsx:409-436`. Web additionally blocks self-enquiry and fires a Meta Pixel `Lead` event (`VehicleDetailsPageClient.tsx:448-482`); the self-enquiry guard was not confirmed on mobile. |
| BUY-028 | BUY | Checkout | Auction buyer-fee checkout | `src/app/checkout/page.tsx:16,63-86` | `POST /payments/checkout` (`payments.controller.ts:30-47`) | DIVERGENT | functionality | M | P1 | Web charges a fixed `AUCTION_BUYER_FEE = 125` (`checkout/page.tsx:16`) via Stripe Checkout redirect. Mobile uses the native Stripe Payment Sheet via `POST /payments/intent` instead (`screens/main/PurchaseFlowScreen.tsx:101-177`, `src/lib/paymentsApi.ts:34-45`) — a correct native adaptation, different endpoint. **Latent fee mismatch:** `PurchaseFlowScreen` defaults `buyerFee` to **95** (`PurchaseFlowScreen.tsx:74,80`) while web's constant is **125** (`checkout/page.tsx:16`). All five live callers pass 125 explicitly (`BuyerBidsScreen.tsx:376`, `AuctionCompleteScreen.tsx:82`, `ChatScreen.tsx:454`, `AuctionDetailScreen.tsx:422,999`), so the £95 path is **unreachable today** — this is a stale default, not an active mispricing. Worth removing so a future caller cannot silently undercharge. See OQ-9. |
| BUY-029 | BUY | Checkout | Retail deposit / full-payment checkout | `src/app/checkout/page.tsx:327-494` | `POST /payments/checkout` | MISSING | functionality | — | P3 | Web has this fully coded (`DEPOSIT_AMOUNT = 500`, full-price mode) but the agent found **no inbound link** reaching `mode=deposit` or `mode=full`; retail purchase is stated as off-platform on the detail pages. Mobile's `PurchaseFlowScreen` likewise supports `DEPOSIT`/`FULL_PAYMENT` but only `COMMISSION` call sites were found. So both apps carry dormant retail-checkout code. Confirm it is intentionally dormant before scoping — see OQ-10. **DECIDED: OUT OF SCOPE.** Vehicle payments are off-platform; the dormant retail-checkout code in both apps is leftover from a bug and should be removed, not wired up (OQ-10). |
| BUY-030 | BUY | Checkout | Payment success / cancel handling | `src/app/checkout/success/page.tsx:52-61`; `src/app/checkout/cancel/page.tsx:69-90` | `GET /payments/session-status/:sessionId`, `POST /payments/apply-*-fee` | PARTIAL | state | M | P1 | Web polls session status and fires webhook-fallback calls per `metadata.type` (`success/page.tsx:52-61`), and its cancel page warns about the 72-hour auction payment deadline (`cancel/page.tsx:69`). Mobile's success path only refetches the auction non-fatally (`PurchaseFlowScreen.tsx:165-171`) and calls **no** `apply-*-fee` fallback — so if the Stripe webhook is delayed, mobile has no recovery path where web does. Mobile also has no cancel screen or 72-hour warning. |
| BUY-031 | BUY | Compare | Compare vehicles | `src/app/compare/page.tsx:99-115,296` | `GET /listings` | PARTIAL | functionality | S | P3 | Both exist. Mobile `CompareScreen.tsx:124` seeds its picker from a static `searchListings({limit:30, sortBy:'newest'})` pool rather than a live search; whether typing triggers a real backend search was **not** confirmed (picker state at L114-116, the call itself not read). |
| BUY-032 | BUY | Detail | Two web detail routes (context for porting) | `src/app/vehicle/[id]/page.tsx:19-25,40-45` vs `src/app/buy-cars/[slug]/page.tsx` | `GET /listings/:slug` (both) | PRESENT | — | — | — | Not a mobile gap — recorded so Phase 2 ports from the right source. Both resolve the param as a **slug** against the same endpoint. `/vehicle/[id]` is explicitly legacy per its own comment (`vehicle/[id]/page.tsx:19-25`), sets `robots: {index:false}` and canonicals to `/buy-cars/` (L40-45). It nonetheless still carries UI the canonical page lacks (a simulated `BidModal` with client-seeded fake bid history, an `EnquireModal`, and a dead "Unlock Full Report for £9.99" button with no onClick). **Port from `/buy-cars/[slug]` only.** See OQ-11. |
| SELL-001 | SELL | Create | Two separate web create implementations | `src/app/sell/page.tsx:50`; `src/app/dashboard/seller/add-listing/page.tsx:22`; `src/app/dashboard/dealer/add-listing/page.tsx:20` | `POST /listings` | PRESENT | — | — | — | Context row, not a mobile gap. `/sell` and `/dashboard/seller/add-listing` both render `ListingWizard` (3200 lines). `/dashboard/dealer/add-listing` renders a **different** component, `DealerQuickList` (969 lines) — a single-scroll form with no legal declarations, a 1-photo minimum, and no auction scheduling. Mobile has one flow (`SellCarFlowScreen.tsx`) that mirrors `ListingWizard`. **Port from `ListingWizard` only** — see OQ-13. |
| SELL-002 | SELL | Create | Sell flow entry point | `src/app/sell/page.tsx:22-49` (dealer KYC gate) | — | DIVERGENT | navigation | S | P2 | Web gates `/sell`: an unverified DEALER sees a "Dealer Verification Required" screen instead of the wizard (`sell/page.tsx:22-49`). Mobile registers `SellCarFlow` (`MainStackNavigator.tsx:278-279`) with **no gate at all** — no `withDealerGate`, no role check at any of the 12 call sites. Mobile also has **no Sell tab**; the flow is reachable only via buttons on other screens. |
| SELL-003 | SELL | Create | Wizard step structure | `src/components/listing/ListingWizard.tsx:517-532` | — | PRESENT | — | — | — | Both are Details → Media → Pricing → (Auction) → Review, with step count 4 for classified and 5 for auction. Mobile `SellCarFlowScreen.tsx:35,1153,1614-1616`. Web adds a cosmetic step 0 "Method" landing screen that mobile folds into the Pricing step's listing-method toggle (`SellCarFlowScreen.tsx:2572-2598`). |
| SELL-004 | SELL | Create | Required fields on the Details step | `src/components/listing/ListingWizard.tsx:598-609` | `CreateListingDto` (`backend/src/listings/dto/create-listing.dto.ts:117-481`) | DIVERGENT | data | S | P1 | Web requires `vrm`, `make`, `model`, `year`, `mileage`, **`fuelType`**, **`transmission`**, `title` (≥5 chars), `location`, `owners` (`ListingWizard.tsx:598`). Mobile's `STEP1_FIELD_KEYS` (`SellCarFlowScreen.tsx:907`) omits **`vrm`, `fuelType` and `transmission`** — all three are optional on mobile. Web also enforces `title.length >= 5`, matching the backend's `@Length(5,200)` (`create-listing.dto.ts:119-122`); mobile only checks non-empty (`SellCarFlowScreen.tsx:862-864`), so a 1-4 char title passes the client and is rejected by the server. |
| SELL-005 | SELL | Create | Photo minimum before publish | `src/components/listing/ListingWizard.tsx:611` | `POST /listings/:id/publish` — **requires ≥10 images** (`backend/src/listings/listings.service.ts:941-945`) | DIVERGENT | data | S | P0 | **Mobile charges the card, then fails to publish.** Backend rejects publish with a 400 when `images.length < 10`. Web enforces ≥10 at the Media step *before* payment (`ListingWizard.tsx:611`). Mobile blocks only at **zero** photos (`SellCarFlowScreen.tsx:1344-1347`), then runs: create listing → `triggerListingFeePayment` (Stripe PaymentSheet, £1/£10/£25) → `POST /listings/:id/publish` (`SellCarFlowScreen.tsx:1528-1545`). A seller with 1-9 photos is charged and the publish call then 400s. See SELL-006 for what they are shown. |
| SELL-006 | SELL | Create | Publish-failure messaging after payment | n/a | — | DIVERGENT | state | S | P0 | Consequence of SELL-005, logged separately because it is its own defect. The publish `catch` shows "Almost there! ... Visit My Listings to publish" (`SellCarFlowScreen.tsx:1544-1550`) but **does not return** — execution falls through to `clearDraft()` and a second alert reading **"Published! / Your listing is now live."** (`SellCarFlowScreen.tsx:1551-1557`). So after a failed publish the seller sees two contradictory alerts, the last of which is false, and the draft is cleared. |
| SELL-007 | SELL | Create | DVLA registration lookup | `src/components/listing/ListingWizard.tsx:1338-1436` | `POST /dvla/lookup` — **no guard, public** (`backend/src/dvla/dvla.controller.ts:30`) | PRESENT | — | — | — | Mobile `SellCarFlowScreen.tsx:941-977` auto-fires once the VRM reaches 7-8 chars (`:931-937`) and populates the same ~18 fields. Both allow manual entry if lookup fails. Mobile arguably better here: web requires an explicit button press. Resolves the standing "DVLA endpoint auth" ambiguity — the endpoint is unauthenticated on the backend. |
| SELL-008 | SELL | Create | Media upload mechanism | `src/components/listing/ImageUpload.tsx:103-163` -> `src/lib/supabase.ts:161-207` | Supabase Storage direct (bucket `listings`) | PRESENT | — | — | — | Both upload client-side direct to Supabase Storage, not through the API. Mobile `src/lib/storageHelper.ts:60-84` via `expo-image-picker` + base64 → ArrayBuffer. Mobile additionally compresses to ≤1.2MB before upload (`storageHelper.ts:16-42`, iterative quality 0.85→0.3); web performs **no** compression. Mobile is better here. |
| SELL-009 | SELL | Create | Photo reordering and cover selection | `src/components/listing/ImageUpload.tsx:206-246,434-439` | — | MISSING | functionality | M | P1 | Web supports HTML5 drag-to-reorder of the image array (`ImageUpload.tsx:206-246`) with index 0 badged "Cover". Mobile's Step 2 grid has **no reorder affordance** — only a remove (X) per thumbnail (`SellCarFlowScreen.tsx:2259-2291`), and index 0 of `exteriorImages` is always labelled COVER (`:2284`) with no way to promote another image except by deleting the ones before it. |
| SELL-010 | SELL | Create | Image size and count enforcement | `src/components/listing/ImageUpload.tsx:63,96-98` | — | PARTIAL | data | S | P2 | Web enforces `MAX_FILE_SIZE = 5MB` and JPEG/PNG/WebP (`ImageUpload.tsx:63-64`) — though its error copy wrongly says "must be less than 20MB" (`:98`), a web-side copy bug. Mobile enforces **neither**: its hint text claims "Max 50 photos" and "Max 5MB per file" (`SellCarFlowScreen.tsx:2253-2254`) but nothing checks either; `launchImageLibraryAsync` has no `selectionLimit` and no size check gates `handlePickPhoto`. Mitigated in practice by mobile's compression step (SELL-008). |
| SELL-011 | SELL | Create | Video URL validation | `src/components/listing/ListingWizard.tsx:1908` | `videoUrls` (`create-listing.dto.ts:195-199`) | PARTIAL | data | S | P3 | Both cap at 5 URLs. Web regex-restricts to YouTube/Instagram/Facebook/X (`ListingWizard.tsx:1908`). Mobile only checks `startsWith('http')` (`SellCarFlowScreen.tsx:1141-1148`) despite showing the same platform hint text (`:2314-2315`). Backend validates only `@IsString({each:true})`, so any string passes server-side. |
| SELL-012 | SELL | Create | Damage mapping / condition | `src/components/listing/ListingWizard.tsx:1822-1944` (`VehicleDamageMapper`) | `POST /damage/:listingId/save` (`backend/src/damage/damage.controller.ts`) | PRESENT | — | — | — | Both present. Mobile uses a WebView + Three.js 3D mapper (`SellCarFlowScreen.tsx:364-520`, `src/components/damage/ThreeDVehicleViewer.tsx`). Backend recomputes `Listing.exteriorGrade` from the saved detections. **Backend note:** `POST /damage/:listingId/save` performs **no ownership check** — any authenticated user can overwrite any listing's damage records (`damage.service.ts` `saveDamageRecords` deletes and recreates by `listingId` alone). Flagged as a backend security issue, not a parity gap — see OQ-15. |
| SELL-013 | SELL | Create | Pricing — min / asking / max | `src/components/listing/ListingWizard.tsx:723-726` | `priceMin` / `price` / `priceMax` (`create-listing.dto.ts:125-142`) | PRESENT | — | — | — | Identical semantics on both: `priceMin` is an optional "offers welcome" floor, `priceMax` is always set equal to the asking price (web `ListingWizard.tsx:725-726`, mobile `SellCarFlowScreen.tsx:1383-1386`). Neither exposes an independent ceiling despite the DTO supporting one. Mobile's `priceMin <= priceAsking` indicator is advisory and does **not** block Next (`SellCarFlowScreen.tsx:2441-2461`); web hard-blocks (`ListingWizard.tsx:612-618`). Minor. |
| SELL-014 | SELL | Create | Auction schedule fields | `src/components/listing/ListingWizard.tsx:2778-2939` | `POST /auctions`; `POST /listings/:id/also-auction` | PRESENT | — | — | — | Mobile has the full Step 4 (`SellCarFlowScreen.tsx:2672-2779`): start NOW/SCHEDULED, reserve, starting bid, min increment (default 100), optional Buy It Now. Both enforce the 70%-of-asking starting-bid cap client-side, correctly mirroring the backend (`listings.service.ts:1218-1223`). 24h duration is fixed and display-only on both. This resolves the standing "canonical auction duration" ambiguity: **24 hours, hardcoded** (`listings.service.ts:1229`). |
| SELL-015 | SELL | Create | Legal declarations | `src/components/listing/ListingWizard.tsx:2264-2458` | `stolenRecovered`, `hasOutstandingFinance`, `isLegalRegisteredKeeper`, `writeOffCategory` (`create-listing.dto.ts:350-373`) | PRESENT | — | — | — | Full parity. Mobile `SellCarFlowScreen.tsx:2080-2174` requires all five: write-off category, stolen/recovered, outstanding finance, legal registered keeper (+ relationship if No), and the acknowledgement checkbox — all in `STEP1_FIELD_KEYS` (`:907`). Note `DealerQuickList` has **none** of these (SELL-001). |
| SELL-016 | SELL | Create | Cat A/B restricted to auction | `src/components/listing/ListingWizard.tsx:603,2290-2305` | Enforced server-side (`listings.service.ts:224-228,774-785`) | PRESENT | — | — | — | Both block CAT_A/CAT_B on classified listings. Web disables the buttons and hard-blocks in `validateStep`; mobile raises an Alert steering the seller to Auction (`SellCarFlowScreen.tsx:1180-1183`). Backend enforces it independently in both `create` and `update`. |
| SELL-017 | SELL | Create | Badge tiers and prices | `src/components/listing/ListingWizard.tsx:2665-2721` | `BASIC £1 / STANDARD £10 / PREMIUM £25` (`backend/src/payments/payments.service.ts:~90-92`) | PRESENT | — | — | — | Mobile `BADGES` (`SellCarFlowScreen.tsx:134-167`) matches names and prices, and correctly filters the tier list by listing type, forcing `FREE` for auctions (`:2585-2591,2608`) — which mirrors the backend forcing FREE→BASIC for non-auction listings (`listings.service.ts:207`). |
| SELL-018 | SELL | Create | Listing-fee payment mechanism | `src/lib/listingApi.ts:1177-1183` -> Stripe Checkout redirect | `POST /payments/listing-checkout` vs `POST /payments/intent` | DIVERGENT | functionality | — | P3 | Web redirects to hosted Stripe Checkout. Mobile uses the native Stripe PaymentSheet via `POST /payments/intent` with `type: 'LISTING_FEE'` (`SellCarFlowScreen.tsx:1203-1235`, `src/lib/paymentsApi.ts:34-45`). Correct native adaptation, different endpoint — recorded so it is not mistaken for a defect. Amounts are hardcoded client-side on mobile (`:1203`), which is a drift risk if backend pricing changes. |
| SELL-019 | SELL | Create | HPI report upsell (£9.99) | `src/components/listing/ListingWizard.tsx:1057-1139` | `POST /payments/hpi-checkout` | PRESENT | — | — | — | Mobile has it on the Review step (`SellCarFlowScreen.tsx:1240-1272`), creating a DRAFT listing first if none exists (`:1283-1303`) and polling `GET /hpi/listing/:id/summary` 6×2s (`:1312-1328`). Not present in `DealerQuickList`. |
| SELL-020 | SELL | Create | Draft persistence | `src/components/listing/ListingWizard.tsx:497-501` (localStorage) | — | PARTIAL | state | M | P1 | Web autosaves the **entire** `formData` to `localStorage['carmazium_listing_draft']` on every change (`ListingWizard.tsx:497-501`). Mobile persists to AsyncStorage via Zustand (`src/lib/sellWizardStore.ts:50-80`) and survives restart, with a "Resume draft?" prompt (`SellCarFlowScreen.tsx:677-718`) — but `partialize` saves only **12 fields**: make, model, year, mileage, title, fuelType, transmission, bodyType, colour, price, listingType, image arrays, lastStep. Legal declarations, DVLA fields, pricing detail, delivery, features and damage records are **all lost** on resume, and the seller must re-enter every mandatory declaration. |
| SELL-021 | SELL | Manage | Listings management screen | `src/app/dashboard/dealer/inventory/page.tsx:123,285` | `GET /listings/my` | PRESENT | — | — | — | Mobile `SellerListingsScreen.tsx:213-224`. Note web's `src/app/dashboard/seller/listings/page.tsx` is a **12-line dead redirect stub** to `/dashboard/user?tab=inventory`; the real screen is the dealer inventory page. |
| SELL-022 | SELL | Manage | Status tabs | `src/app/dashboard/dealer/inventory/page.tsx:285` | — | PARTIAL | functionality | S | P2 | Web offers 6 tabs: ALL, ACTIVE, PENDING_REVIEW, REJECTED, DRAFT, SOLD (`inventory/page.tsx:285`), filtered client-side. Mobile offers 4: ALL, ACTIVE, DRAFT, SOLD (`SellerListingsScreen.tsx:100`), filtered server-side via `?status=`. **Mobile has no PENDING_REVIEW or REJECTED tab** — since `publishListing` moves listings to PENDING_REVIEW and admins can set REJECTED, a mobile seller has no way to see that a listing was rejected or why (`rejectionReason` is never surfaced). |
| SELL-023 | SELL | Manage | Withdraw listing | *(not found on web)* | `PATCH /listings/:id/status {status:'WITHDRAWN'}` | PRESENT | — | — | — | Reverse gap — **mobile is ahead**. Mobile exposes Withdraw on ACTIVE listings (`SellerListingsScreen.tsx:123-174`). The backend supports `WITHDRAWN` in `UpdateStatusDto` (`update-status.dto.ts:6,11`), but no Withdraw action was found in any of the four web management screens audited. |
| SELL-024 | SELL | Manage | Mark as sold | `src/app/dashboard/dealer/inventory/page.tsx:189-192` | `PATCH /listings/:id/sold` (`RecordSaleDto`) vs `PATCH /listings/:id/status` | PRESENT | — | — | — | Reverse gap — **mobile uses the correct endpoint**. Mobile opens a price modal and calls `PATCH /listings/:id/sold {soldPrice}` (`SellerListingsScreen.tsx:123-174`), the dedicated `recordSale` path. Web calls the generic `PATCH /listings/:id/status {status:'SOLD', buyerPostcode}` (`inventory/page.tsx:189-192`), so web **never captures `soldPrice`** and the server falls back to the list price. The `RecordSaleDto` buyer fields (`buyerId`, `buyerName`, `buyerEmail`) are unused by both. |
| SELL-025 | SELL | Manage | Relist a sold/withdrawn listing | `src/app/dashboard/dealer/inventory/page.tsx:354-357` | `PATCH /listings/:id/status {status:'ACTIVE'}` | PRESENT | — | — | — | Mobile `SellerListingsScreen.tsx:123-174` (SOLD/WITHDRAWN → Relist). Backend permits ACTIVE here only when the current status is already ACTIVE/SOLD/WITHDRAWN/OFFER_ACCEPTED (`listings.service.ts:873-877`), which both clients respect by only offering it from those states. Neither confirms before relisting. |
| SELL-026 | SELL | Manage | Delete listing | `src/app/dashboard/dealer/inventory/page.tsx:167-176` | `DELETE /listings/:id` (soft delete) | PRESENT | — | — | — | Both confirm first (web `window.confirm`, mobile Alert). Soft delete only — sets `deletedAt` (`listings.service.ts:1303-1321`). |
| SELL-027 | SELL | Manage | Boost to featured | `src/app/dashboard/dealer/inventory/page.tsx:391-403` | `POST /featured-boost/:listingId` — £25 / 28 days | PRESENT | — | — | — | Mobile offers it on ACTIVE listings from two screens (`SellerListingsScreen.tsx:123-174`, `MyListingDashboardScreen.tsx:150-173`), opening Stripe checkout in an in-app WebView. Backend restricts to ACTIVE, not-already-featured listings (`featured-boost.service.ts:27-45`). |
| SELL-028 | SELL | Manage | Publish a DRAFT from the manage screen | `src/app/dashboard/dealer/inventory/page.tsx:134-165` | `POST /listings/:id/publish` | PRESENT | — | — | — | Mobile does this correctly and in the **right order**: calls publish first, and only runs the Stripe sheet if the response returns `requiresPayment` (`SellerListingsScreen.tsx:331-380`). That ordering avoids the SELL-005 defect — note the create flow does *not* use it. Web's inventory page goes straight to `createListingCheckoutSession` with no visible publish call. |
| SELL-029 | SELL | Manage | Also-list-retail / also-auction cross-listing | `src/app/dashboard/dealer/inventory/page.tsx:202-215,404-411`; `src/app/dashboard/dealer/put-on-auction/page.tsx:76-82` | `POST /listings/:id/also-auction`, `/also-list-retail` | PRESENT | — | — | — | Mobile has both: also-auction from `SellerListingsScreen.tsx:448-490` and a create flow in `SellerAuctionsScreen.tsx:707-756`. Both enforce the 70% starting-bid cap client-side. **Backend note:** `POST /listings/:id/also-auction` uses per-field `@Body('x')` extraction with **no DTO class** (`listings.controller.ts:387-405`), so none of the validation decorators apply — only the service's manual checks. |
| SELL-030 | SELL | Manage | Import listing from URL | *(not found in audited web screens)* | `POST /listings/import-from-url`, `/preview-import` | PRESENT | — | — | — | Mobile exposes both a Bulk Import and an Import-from-URL modal in the `SellerListingsScreen` header (`SellerListingsScreen.tsx:716-719`). No web equivalent was found in the four management screens read — possibly elsewhere in web; not asserted as a web gap. |
| SELL-031 | SELL | Manage | Draft completeness gate | `src/app/dashboard/dealer/inventory/page.tsx:26-44` | `publishListing` checks images ≥10 only (`listings.service.ts:941-945`) | DIVERGENT | data | S | P2 | Web's client-side completeness check requires `images >= 1`, `transmission`, `bodyType`, `description`, `condition` (`inventory/page.tsx:26-44`) — which matches **neither** the backend gate (10 photos, nothing else) nor its own wizard (10 photos + declarations). Three different definitions of "complete" across one product. Mobile has no equivalent gate. |
| SELL-032 | SELL | Create | `exteriorGrade` is server-computed | n/a | Hardcoded to 1 at create (`listings.service.ts:308`), recomputed from damage records | PRESENT | — | — | — | Neither client sets it, correctly. Recorded because both apps *display* a grade chip on cards (Pass 2 BUY-005), so its provenance matters: it comes from `POST /damage/:listingId/save`, not from the seller. |
| AUC-001 | AUCTION | Realtime | WebSocket transport | `src/app/auctions/live/[id]/page.tsx:220-225` | Gateway namespace `/auctions` (`backend/src/auctions/auction.gateway.ts:31-34`) | PRESENT | — | — | — | Both connect to the same `/auctions` namespace with `transports:['websocket']`, `reconnectionAttempts:5`, `reconnectionDelay:2000`. Mobile `screens/vehicle/AuctionDetailScreen.tsx:354-370`. Genuine real-time parity — this is not a polling-vs-sockets gap. |
| AUC-002 | AUCTION | Realtime | Socket authentication | `src/app/auctions/live/[id]/page.tsx:220-225` (`withCredentials`, cookie) | — | DIVERGENT | state | — | P3 | Web authenticates the socket via cookies (`withCredentials: true`). Mobile uses the function-form `auth` callback so a fresh token is fetched on every reconnect attempt (`AuctionDetailScreen.tsx:360-362`). Correct platform adaptation — mobile has no cookie jar. Recorded so it is not mistaken for a defect. |
| AUC-003 | AUCTION | Realtime | Socket event coverage | `src/app/auctions/live/[id]/page.tsx:228-296` | 6 server-emitted events (`auction.gateway.ts:52-100`) | PRESENT | — | — | — | Both handle the full set: `auction:viewers`, `bid:new`, `bid:cancelled`, `bin:pending`, `auction:ended`, `auction:started`, plus `connect`/`disconnect`/`reconnect`. Mobile `AuctionDetailScreen.tsx:370-462`. Both emit only `auction:join`; bids go over REST on both. |
| AUC-004 | AUCTION | Realtime | Reconnect does not resync state | `src/app/auctions/live/[id]/page.tsx:233-235` | `auction:join` re-subscribes only (`auction.gateway.ts:65-75`) | NEEDS_VERIFICATION | state | M | P1 | Shared weakness, worse on mobile. Neither client re-fetches auction state on reconnect — both only re-emit `auction:join`, which subscribes to the room but replays nothing. Any bid, cancellation or `auction:ended` that fired during the gap is lost. Web at least offers a page reload; mobile's `AuctionDetailScreen` has **no manual refresh control at all**, so a missed `auction:ended` can leave the screen showing ACTIVE with a frozen `00:00:00` timer indefinitely. See OQ-17. **FIXED on mobile 2026-09-01 (Flow 2a) — awaiting device verification.** The `reconnect` handler now refetches auction state alongside re-emitting `auction:join` (`AuctionDetailScreen.tsx:396-404`). `loadAuction` gained a `{ silent: true }` mode so the resync does not blank the live screen behind a full-page loader (`:291-297`), and it is reached through a ref (`:342-350`) so the socket effect keeps its `[auctionId, currentUser?.id]` deps — taking the callback as a dependency would have reintroduced the documented mid-auction socket teardown. **Web is unchanged and still has this gap** (OQ-17 scoped the fix to mobile). |
| AUC-005 | AUCTION | Realtime | Disconnect messaging | `src/app/auctions/live/[id]/page.tsx:827-842` | — | PRESENT | — | — | — | Both show a connection-lost banner while `!connected && isLive` (mobile `AuctionDetailScreen.tsx:898-903`) plus a connection dot in the header. Neither has a polling fallback — equal behaviour, so not a mobile gap. |
| AUC-006 | AUCTION | Timers | Countdown rendering | `src/components/features/CountdownTimer.tsx:21-50` | — | PRESENT | — | — | — | Both tick at 1s and render `HH:MM:SS`, freezing at zero without firing any end-of-auction logic (end comes only from the socket). Mobile `AuctionDetailScreen.tsx:80-86,500-508`, with an extra under-5-minutes pulse animation (`:257-273`). |
| AUC-007 | AUCTION | Timers | Anti-snipe extension reflected live | `src/app/auctions/live/[id]/page.tsx:246-252` | `newEndTime` on `bid:new` (`auction.gateway.ts:13-22`); `maybeExtend` (`backend/src/auctions/auctions.service.ts:751-769`) | PRESENT | — | — | — | Both apply `payload.newEndTime` to the countdown immediately and show a toast plus a persistent info bar. Mobile `AuctionDetailScreen.tsx:398-404,798-804,1106-1113`. Backend rule confirmed: `ANTI_SNIPE_MINUTES = 3` (`auctions.service.ts:20`), extends by 3 min when `0 < timeLeft <= 3min`, **repeats with no cap**. |
| AUC-008 | AUCTION | Timers | Stale 10-minute anti-snipe helper | `src/lib/auctionApi.ts:268-272` | Actual rule is 3 minutes (`auctions.service.ts:20`) | PRESENT | — | — | — | **Web-side bug, not a mobile gap** — logged so mobile does not copy it. `isAntiSnipeActive()` uses `const tenMinutes = 10 * 60 * 1000` and drives the "SNIPE" badge on the auction browse page (`src/app/auctions/page.tsx:193`). So web's browse badge lights up at 10 minutes while the live room's own logic uses 3 (`live/[id]/page.tsx:306`) and the backend uses 3. Mobile's browse screen has no such badge, so mobile is accidentally correct. |
| AUC-009 | AUCTION | Bidding | Bid input and quick-bid buttons | `src/app/auctions/live/[id]/page.tsx:1641-1683` | `POST /bids` (`backend/src/bids/bids.controller.ts:35-49`) | PRESENT | — | — | — | Both offer 4 quick-bid buttons (`+increment`, ×2, ×5, ×10) plus a custom amount field with submit-on-enter. Mobile `AuctionDetailScreen.tsx:722,1695-1737`. Neither does optimistic updates — both wait for the `bid:new` echo. |
| AUC-010 | AUCTION | Bidding | Client-side bid validation | `src/app/auctions/live/[id]/page.tsx:339-368` | Min-increment rule (`bids.service.ts:66-88`) | PRESENT | — | — | — | Mobile is slightly stricter and closer to the server: it checks amount > 0, > currentBid, **and** `>= currentBid + minIncrement` (`AuctionDetailScreen.tsx:535-538`). Web checks > 0, > currentBid and `>= startingBid` but **not** the min-increment (`live/[id]/page.tsx:339-368`), relying on the server's 400. Minor reverse gap in mobile's favour. |
| AUC-011 | AUCTION | Bidding | Bidder eligibility — verified dealers only | `src/app/auctions/live/[id]/page.tsx:1623` (`profile?.role === 'DEALER'`) | **`role === 'DEALER' && dealerProfile.isVerified`** (`backend/src/bids/bids.service.ts:57-64`) | PRESENT | — | — | — | Backend throws `ForbiddenException('Only verified dealers can place bids on auctions.')`. Mobile gates the console on both role **and** `isVerified`, and offers a KYC prompt when unverified (`AuctionDetailScreen.tsx:513-532`) — better than web, which checks role only. **Cross-reference AUTH-005:** mobile signup hardcodes BUYER, so a mobile-registered user must go through `DealerOnboardingScreen` → `POST /users/elevate` → KYC before they can bid at all. That path exists on mobile (`MainStackNavigator.tsx:205-218`), so it is a longer road, not a dead end. |
| AUC-012 | AUCTION | Bidding | Bidder identity in bid history | `src/app/auctions/live/[id]/page.tsx:176` (initials only) | Backend returns full `bidder.firstName`/`lastName` | NEEDS_VERIFICATION | permissions | S | P1 | **Mobile discloses competing bidders' full names.** Both receive the same payload, but web maps it to `initials` and discards the rest (`live/[id]/page.tsx:176`). Mobile builds `fullName` from `firstName`/`lastName` and renders it in each bid row (`AuctionDetailScreen.tsx:296-306`, rendered at `:1369`). On a dealer-to-dealer auction platform where web deliberately shows only initials, this exposes who a dealer is bidding against. Note live-arriving bids carry only `bidderInitials` over the socket, so mobile shows initials for those and full names for the ones loaded initially — inconsistent within the same list. See OQ-18. **FIXED 2026-09-01 (Flow 2a, `parity/auction-correctness`) — awaiting device verification.** The `name` field was **removed from `BidEntry` entirely** rather than reassigned, so it cannot quietly return (`AuctionDetailScreen.tsx:50-62`); the initial-load mapper no longer builds a full name (`:305-313`), the socket mapper drops its duplicate `name` (`:405`), and the row renders `bid.initials` (`:1403`). Matches web, which renders initials in both the avatar and the label (`live/[id]/page.tsx:1360,1362`). |
| AUC-013 | AUCTION | Bidding | Cancel own bid within 24h | `src/app/auctions/live/[id]/page.tsx:408-425,1520-1549` | `PATCH /bids/:id/cancel`, 24h window (`bids.service.ts:16,152-182`) | PRESENT | — | — | — | Both offer it. Mobile exposes it from the live screen (`AuctionDetailScreen.tsx:557-582`) **and** from `BuyerBidsScreen.tsx:315-340`; web offers it only in the live feed. Backend requires bid ownership, not already cancelled, and parent auction still ACTIVE. |
| AUC-014 | AUCTION | Bidding | Winning / outbid indicator | `src/app/auctions/live/[id]/page.tsx:918-927` | — | PRESENT | — | — | — | Both show WINNING/OUTBID badges recomputed on every `bid:new` and `bid:cancelled`. Mobile `AuctionDetailScreen.tsx:1061-1072`; also per-row chips in `BuyerBidsScreen.tsx:411-427`. Neither shows an in-page outbid toast; both rely on the notification system. |
| AUC-015 | AUCTION | Reserve | Reserve hidden from buyers | `src/app/auctions/live/[id]/page.tsx:495-497` | `reservePrice` **is** returned by the API (`auctions.service.ts:240-287`) | PRESENT | — | — | — | Neither client displays the reserve to buyers; both hide it purely client-side. Mobile documents this explicitly in comments at `AuctionDetailScreen.tsx:1098-1100,1296-1298`. **Backend note:** `findOne` does not strip `reservePrice` from the response the way it gates seller contact details, so the value is on the wire and visible to anyone inspecting network traffic. Flagged in OQ-19 — backend concern, not a parity gap. **DECIDED:** reserve is internal-only; follow web. No mobile change. Backend still returns `reservePrice` in `findOne`, so confidentiality is presentational only — noted, not actioned (OQ-19). |
| AUC-016 | AUCTION | Reserve | Seller reserve status + adjust control | *(no web equivalent found)* | — | DIVERGENT | functionality | S | P2 | Mobile shows sellers a reserve-met/not-met bar with the actual figure (`AuctionDetailScreen.tsx:1613-1625`) — useful and not present on web. But the accompanying "Adjust Reserve Price" button is a **non-functional stub**: it shows `Alert.alert('Reserve Updated', ...)` and makes **no API call** (`AuctionDetailScreen.tsx:1628-1644`). It tells the seller their reserve changed when nothing changed. Either wire it to `PATCH /auctions/:id` or remove it. |
| AUC-017 | AUCTION | Buy It Now | Buyer BIN trigger | `src/app/auctions/live/[id]/page.tsx:497,1696-1710` | `POST /auctions/:id/bin-trigger` (`auctions.controller.ts:190-200`) | PRESENT | — | — | — | Closes BUY-018 from Pass 2. Both gate on active + not seller + BIN price set + reserve not met, and both confirm before calling. Mobile `AuctionDetailScreen.tsx:586-610,1496-1532`. Neither gates the BIN button on dealer role, unlike the bid console — backend preconditions do not check role either (`auctions.service.ts:1032-1070`), so BIN appears open to non-dealers on both. Consistent across platforms, so not a mobile gap; noted for the record. |
| AUC-018 | AUCTION | Buy It Now | Seller confirm / decline | `src/app/auctions/live/[id]/page.tsx:1718-1741` | `bin-confirm` / `bin-decline` (`auctions.controller.ts:202-225`) | PRESENT | — | — | — | Both present. Mobile `AuctionDetailScreen.tsx:612-648,904-938`. Both react to the `bin:pending` socket event. Backend auto-cancels a pending BIN if a bid meets or exceeds the BIN price (`bids.service.ts:97-117`) and lazily expires it after 24h (`auctions.service.ts:1172-1186`). |
| AUC-019 | AUCTION | End | Auction-end handling in UI | `src/app/auctions/live/[id]/page.tsx:747-825` | `auction:ended` payload (`auction.gateway.ts:24-29`) | PRESENT | — | — | — | Both render four outcome branches (won / reserve not met / ended with another winner / no bids). Mobile `AuctionDetailScreen.tsx:956-1035`. Mobile additionally auto-navigates the winner to `AuctionComplete` on the event (`:409-438`) — a native-appropriate improvement over web's in-place banner. |
| AUC-020 | AUCTION | Post-auction | Won-auction destination | `src/app/auctions/won/[id]/page.tsx:113-128` | `GET /auctions/my/won` | PARTIAL | functionality | M | P2 | Web's won page is fee-gated (redirects back to the live room unless `buyerFeePaid`) and shows unblurred seller contact, an embedded `ChatWindow`, a 3-stage handover status card driven by real fields, and an auction summary. Mobile's `AuctionCompleteScreen.tsx` covers payment and a review form but its "YOUR JOURNEY" list is **static** (`:303-464`) — the handover steps are hardcoded and never reflect `handoverProofUrl` or `sellerBonusReleased`. Mobile has no equivalent of web's live handover status card. |
| AUC-021 | AUCTION | Post-auction | Buyer fee £125 payment | `src/app/auctions/live/[id]/page.tsx:769-782` -> `/checkout?mode=auction_fee` | `BUYER_FEE_GRACE_MS = 72h` (`auctions.service.ts:23`) | PRESENT | — | — | — | Mobile uses the native PaymentSheet with `type:'COMMISSION'` and then polls `GET /auctions/:id` up to 5×1.5s to confirm the webhook flipped `buyerFeePaid` (`AuctionCompleteScreen.tsx:173-263`). That webhook-confirmation poll is **better than web**, which has no equivalent confirmation step. |
| AUC-022 | AUCTION | Post-auction | Payment deadline semantics | n/a | 72h grace, enforced by `revertUnpaidWins` hourly cron (`auctions.service.ts:618-689`) | NEEDS_VERIFICATION | data | S | P1 | **Mobile shows the wrong deadline, and it disagrees with itself.** The backend reverts an unpaid win after **72 hours**. `AuctionCompleteScreen` falls back to **24 hours from mount** when no `paymentDeadline` is passed (`:98-115`). The socket-driven win path passes `paymentDeadline: undefined` (`AuctionDetailScreen.tsx:434`), so it always uses that 24h fallback; `BuyerBidsScreen.tsx:115-117` instead computes `auction.endTime + 24h`. Three different numbers, none of them 72h. A winner may be told they have hours left when they have days, or vice versa. **FIXED 2026-09-01 (Flow 2a) — awaiting device verification.** One shared `AUCTION_PAYMENT_GRACE_MS = 72h` now lives in `lib/auctionApi.ts:5-20`, citing the backend constant. The socket win path passes `Date.now() + 72h` (`AuctionDetailScreen.tsx:463-469`) — **exact**, because the backend sets `wonAt` at the moment that event fires (`auctions.service.ts:544`). `BuyerBidsScreen.tsx:115-124` (`:122`) passes `endTime + 72h`. The 24h mount-time fallback is **deleted, not re-pointed**: `timeLeft` is now `number | null` and with no deadline the countdown is not rendered at all rather than invented (`AuctionCompleteScreen.tsx:97-123,534-563`); payment stays enabled in that case, since blocking a payment on a deadline we do not know would be worse. **Known residual, logged as a backend item:** the grace runs from `wonAt`, but `GET /bids/my` does not select it (`bids.service.ts:203-211`), so the bids-screen figure is an approximation off `endTime` where the live-win path is exact. |
| AUC-023 | AUCTION | Post-auction | Handover proof upload (seller) | `src/app/dashboard/seller/auctions/page.tsx:352-355,1071-1102` | `POST /auctions/:id/handover-proof` (`auctions.controller.ts:227-245`) | PARTIAL | functionality | M | P2 | Same endpoint, same storage path convention (`handover/…` in the `listings` bucket). Mobile `SellerAuctionsScreen.tsx:292-321`. Two gaps: mobile uses `ImagePicker` and accepts **images only**, where web's dropzone accepts `image/*,.pdf`; and mobile has no equivalent of web's sticky "N auctions waiting on you" banner (`page.tsx:409-428`) prompting sellers to act. |
| AUC-024 | AUCTION | Post-auction | Handover / payout status detail | `src/app/dashboard/seller/auctions/page.tsx:64-101,964-986` | `sellerBonusReleased`, `stripePayoutTransferId`, `manualPayoutConfirmedAt` | PARTIAL | data | M | P2 | Web derives rich states onto the status badge itself — "Action Needed" (pulsing), "Ended · Paid", "Ended · Payout Processing" — and keeps a separate Approved Handovers list distinguishing Processing from Complete. Mobile's `STATUS_CFG` has only the four raw statuses (`SellerAuctionsScreen.tsx:123-128`) and a flat "£100 payout released" pill (`:955-959`), with no processing/complete distinction. Mobile does show `stripePayoutError` (`:948-954`) but with no retry action. |
| AUC-025 | AUCTION | Post-auction | Stripe Connect payout warning | `src/app/dashboard/seller/auctions/page.tsx:137,1001-1013` | `GET /users/stripe-connect/status` | MISSING | functionality | M | P1 | Web warns a seller whose Stripe Connect account is not set up that they cannot receive the £100 bonus, before they do the handover work. Mobile has **no equivalent** — a mobile seller can complete an auction, upload handover proof, and get approved with no payout destination and no warning. |
| AUC-026 | AUCTION | Seller | Auction list tabs and filtering | `src/app/dashboard/seller/auctions/page.tsx:640-961` | `GET /auctions/my/list` | PRESENT | — | — | — | Reverse gap — **mobile is better organised**. Mobile has explicit ALL/LIVE/SCHEDULED/ENDED/WON tabs (`SellerAuctionsScreen.tsx:50,1082-1100`); web renders every status in one flat table with no status filter. |
| AUC-027 | AUCTION | Seller | WON-as-bidder tab | *(no web equivalent)* | `GET /auctions/my/won` (`auctions.controller.ts:71-82`) | DIVERGENT | functionality | — | P3 | Mobile-only. `SellerAuctionsScreen.tsx:234-249` surfaces auctions the user won as a bidder; web's seller auctions page has no such tab. Extra capability, not a gap. |
| AUC-028 | AUCTION | Seller | Live countdown in the auction list | `src/app/dashboard/seller/auctions/page.tsx:36-44,151-154` | — | MISSING | visual | S | P3 | Web ticks a live "ends in" / "starts in" countdown every second on each row. Mobile shows only a static formatted date (`SellerAuctionsScreen.tsx:130-134`). |
| AUC-029 | AUCTION | Seller | Re-auction a reserve-not-met listing | `src/app/dashboard/seller/auctions/page.tsx:196-218` | Listing reverts to DRAFT/CLASSIFIED at close (`auctions.service.ts:845-872`) | NEEDS_VERIFICATION | functionality | S | P0 | **Mobile carries a bug web has already fixed, and web's code comment documents the fix.** When an auction ends below reserve the backend reverts the listing to **DRAFT** so it can be re-auctioned. Web's eligibility filter explicitly admits `status === 'DRAFT'` when an ENDED auction exists for that listing (`page.tsx:212-218`), with a comment stating that without it "re-auctioning was silently impossible for every seller". Mobile still filters `type === 'CLASSIFIED' && status === 'ACTIVE'` (`SellerAuctionsScreen.tsx:631`), so the reverted DRAFT never matches `presetListingId` (`:633-638`) and never appears in the picker. **The mobile "Re-auction" button fails for the exact case it exists to serve.** **FIXED 2026-09-01 (Flow 2a) — awaiting device verification.** `openCreateModal` now fetches `/auctions/my/list` alongside `/listings/my` (web calls this set `freshAuctions`, and freshness matters because the re-auction path arrives straight from the just-ended auction's results modal) and admits `status === 'DRAFT'` when an ENDED auction exists for that listing id (`SellerAuctionsScreen.tsx:626-676`). Ordinary unfinished drafts stay excluded. |
| AUC-030 | AUCTION | Seller | Create-auction eligibility exclusions | `src/app/dashboard/seller/auctions/page.tsx:212-218` | — | NEEDS_VERIFICATION | data | S | P2 | Beyond AUC-029, web also excludes listings already tied to a scheduled/active auction (`!auctionListingIds.has(l.id)`) and those with a `linkedListingId`. Mobile's filter does neither (`SellerAuctionsScreen.tsx:631`), so a seller can select a listing that is already in another auction and get a server-side rejection instead of it simply not being offered. **FIXED 2026-09-01 (Flow 2a) — awaiting device verification.** Folded into AUC-029 because it is the same filter expression, three lines of the same change, and exercised by the same test: listings tied to a SCHEDULED or ACTIVE auction and listings carrying a `linkedListingId` are now both excluded (`SellerAuctionsScreen.tsx:651-675`); `linkedListingId` added to `EligibleListing` (`:118-121`). |
| AUC-031 | AUCTION | Seller | Start-time controls | `src/app/dashboard/seller/auctions/page.tsx:234,502-514` | `startTime` not more than 60s in the past (`auctions.service.ts:1225-1228`) | DIVERGENT | data | S | P2 | Web has a "Start immediately" toggle and otherwise enforces a **+30 minute** minimum lead time (`page.tsx:234`). Mobile has no immediate-start toggle and enforces only **+1 minute** (`SellerAuctionsScreen.tsx:711`). Two different business rules for the same action; the backend accepts both. |
| AUC-032 | AUCTION | Seller | 70% starting-bid validation | *(not validated client-side on web)* | Enforced server-side (`listings.service.ts:1218-1223`) | PRESENT | — | — | — | Reverse gap — **mobile is ahead**. Mobile pre-validates that the starting bid is ≤70% of the asking price before submitting (`SellerAuctionsScreen.tsx:724-730`); web's `handleCreate` does not (`page.tsx:224-262`) and lets the server 400. |
| AUC-033 | AUCTION | Seller | Digest — custom tags and self-rating | `src/app/dashboard/seller/auctions/page.tsx:306-330` | `PATCH /auctions/:id/digest` (`auctions.controller.ts:121-134`) | PRESENT | — | — | — | Same endpoint, same 10-tag cap, offered on ACTIVE and SCHEDULED on both. Mobile uses a chip-based add/remove UI (`SellerAuctionsScreen.tsx:429-459,1246-1325`) versus web's comma-separated text field — mobile's is the better interaction. Minor payload difference: web omits `sellerSelfRating` when falsy, mobile sends `null` (`:436`). |
| AUC-034 | AUCTION | Seller | Close bids / cancel auction | `src/app/dashboard/seller/auctions/page.tsx:264-289` | `POST /auctions/:id/close`, `PATCH /auctions/:id/cancel` | PRESENT | — | — | — | Identical endpoints and identical status gating on both (close only when ACTIVE, cancel only when SCHEDULED), matching the backend (`auctions.service.ts:479-521`). Only the confirmation UI differs (`Alert.alert` vs `confirm()`). Mobile `SellerAuctionsScreen.tsx:326-338,536-560`. |
| AUC-035 | AUCTION | Seller | Resume an unpaid retail cross-listing | `src/app/dashboard/seller/auctions/page.tsx:735-746,913-924` | `POST /payments/listing-checkout` | MISSING | functionality | M | P1 | Web shows a "Resume Retail Payment" action when an also-listed retail listing is stuck in DRAFT because payment did not complete. Mobile has no such path: if the PaymentSheet fails or is cancelled during "Also List for Sale", the draft is created with only an inline note (`SellerAuctionsScreen.tsx:505-507`) and there is **no way back into that payment** — the seller's listing is stranded. Web also hides the also-retail action once a linked listing exists; mobile does not check (`:376-382`). |
| AUC-036 | AUCTION | Seller | Results modal | `src/components/auctions/AuctionResultsModal.tsx:19-46,82-105` | *(no endpoint — derived from list data)* | PARTIAL | functionality | S | P3 | Structure matches closely (stats grid, timeline, view/connect/re-auction actions). Mobile lacks two web features: the contextual "Tips" advice block (`AuctionResultsModal.tsx:19-46`, e.g. gap-to-reserve guidance) and the vehicle summary chip row (`:82-105`). Mobile's bid count also falls back through `_count?.bids ?? bids?.length ?? 0` (`SellerAuctionsScreen.tsx:1373-1375`) and silently shows `0` if neither field is present. |
| AUC-037 | AUCTION | Browse | Auction browse screen | `src/app/auctions/page.tsx` | `GET /auctions/active`, `/auctions/scheduled` | PRESENT | — | — | — | Both are REST-fetched with no socket, both tick a local countdown only. Mobile `screens/main/LiveScreen.tsx:103-189`. Neither reflects a live bid or an auction ending until refetch — equal behaviour, so not a mobile gap. Mobile adds pull-to-refresh; web has no refresh control. |
| AUC-038 | AUCTION | Post-auction | Contradictory handover copy | n/a | — | DIVERGENT | visual | S | P2 | Mobile-only defect. `AuctionCompleteScreen.tsx:325` headers the paid state **"Handover confirmed"**, while the journey list immediately below shows "Handover to be booked" and "Handover confirmed" as *incomplete* (`:303-464`). The header asserts something the same screen contradicts, and no buyer-side action can ever complete those steps — `submitHandoverProof` is seller-only (`SellerAuctionsScreen.tsx:309`). |
| AUC-039 | AUCTION | Contract | Stale 5-hour duration in API docs | n/a | Actual `AUCTION_DURATION_MS = 24h` (`auctions.service.ts:19`) | PRESENT | — | — | — | Documentation defect, no client impact — logged so it stops misleading. `create-auction.dto.ts:10` and `auctions.controller.ts:99` both say "End time is always startTime + 5 hours", but the implementation uses 24 hours, and both clients' UI copy correctly says 24. Confirms the canonical duration is **24 hours**. |
| DASH-001 | DASH | Shell | Web has one unified dashboard plus orphaned standalone routes | `src/app/dashboard/page.tsx:31-44`; `src/components/dashboard/DashboardSidebar.tsx:134-150` | `GET /dashboard/unified` | PRESENT | — | — | — | **Context row — read before any Phase 2 dashboard work.** `/dashboard` routes BUYER and SELLER to `/dashboard/user`, a single tab-driven page. The sidebar links **only** to `/dashboard/user?tab=…` plus `/dashboard/seller/auctions` (`DashboardSidebar.tsx:134-150`; a grep for other `dashboard/buyer`/`dashboard/seller/` hrefs returns just that one line). Yet nine fully-built standalone pages still exist — `/dashboard/buyer/{,history,settings,messages}` and `/dashboard/seller/{,earnings,performance,offers,settings}` — reachable only by direct URL. They are **not** stubs and several are richer than the unified tabs. See OQ-21. |
| DASH-002 | DASH | Shell | Dashboard entry point | `src/app/dashboard/layout.tsx:1-24` | — | DIVERGENT | navigation | — | P3 | Web uses a sidebar shell with 9 tab links. Mobile reaches the dashboard via the Profile tab, which renders `DealerProfileScreen` when `role === 'dealer'` and `UnifiedDashboardScreen` otherwise (`navigation/TabNavigator.tsx:27-30,53`, tab labelled "DASHBOARD"). Correct native adaptation. |
| DASH-003 | DASH | Shell | Profile completion gate | `src/components/dashboard/ProfileCompletionGate.tsx:15-158` | `PATCH /users/me` | MISSING | state | M | P2 | Web blocks **all** dashboard content behind a modal until `firstName`, `lastName` and (non-dealers) `phone` are set (`ProfileCompletionGate.tsx:24-26`). Mobile has no equivalent gate — the post-signup wizard (AUTH-021) collects name but is skippable and does not enforce phone. A mobile user can sit in the dashboard indefinitely with no phone number. |
| DASH-004 | DASH | Buyer | Buyer dashboard stats | `src/app/dashboard/user/page.tsx:244-279` (`OverviewTab`) | `GET /dashboard/unified` (`backend/src/dashboard/dashboard.service.ts:213-228`) | DIVERGENT | data | S | P2 | Both consume `/dashboard/unified`. Web's overview shows four tiles — Active Inventory, Total Revenue, Watchlist, Total Views — mixing seller and buyer figures. Mobile's `UnifiedDashboardScreen.tsx:25-29,115` consumes the same endpoint. **Amended by CROSS-021 (Pass 6):** the richer buyer tile set at `BuyerDashboardScreen.tsx:290-332` (ACTIVE OFFERS, WATCHING, LIVE BIDS, AUCTIONS WON, TOTAL SPENT, sourced from `/dashboard/buyer`) is in a screen that is **unreachable dead code** — zero `navigate('BuyerDashboard')` call sites and not in the drawer. So the tiles a mobile buyer actually sees are only those in `UnifiedDashboardScreen`. Decide one canonical tile set, and decide whether `BuyerDashboardScreen` is revived or deleted. |
| DASH-005 | DASH | Buyer | Period toggle (7d/30d) | `src/app/dashboard/buyer/page.tsx:81` (orphaned route only) | `?period=` on `/dashboard/{buyer,seller,dealer}` (`dashboard.controller.ts:27,37`) | PRESENT | — | — | — | Reverse gap — **mobile is ahead on the live path**. Mobile has the 7D/30D toggle on both `BuyerDashboardScreen.tsx:171,272-286` and `SellerDashboardScreen.tsx:194,290-303`. On web the toggle exists **only** on the orphaned `/dashboard/buyer` and `/dashboard/seller` pages; the unified dashboard users actually reach has no period control at all. |
| DASH-006 | DASH | Buyer | Purchase history | `src/app/dashboard/buyer/history/page.tsx:88-131` (orphaned) | `GET /transactions/my` (`transactions.controller.ts:28-48`) | PRESENT | — | — | — | Mobile `BuyerPurchaseHistoryScreen.tsx:163` — same endpoint, paginated, with a totals bar web lacks. Note there is **no purchase-history tab inside `/dashboard/user`**, so on web this exists only at the orphaned route. Mobile is the more reachable implementation. |
| DASH-007 | DASH | Buyer | Receipt download | `src/app/dashboard/buyer/history/page.tsx:21-30,122-131` | `GET /transactions/:id/receipt.pdf` (`transactions.controller.ts:66-83`) | PRESENT | — | — | — | Mobile `BuyerPurchaseHistoryScreen.tsx:204-228` uses `FileSystem.downloadAsync` with an explicit Bearer header then `Sharing.shareAsync`, with an inline comment explaining it avoids `Linking.openURL` because that drops the auth header. Correct handling of an authenticated binary. |
| DASH-008 | DASH | Seller | Seller dashboard stats | `src/app/dashboard/user/page.tsx:244-279` | `GET /dashboard/seller` (`dashboard.service.ts:261-283`) | PRESENT | — | — | — | Mobile `SellerDashboardScreen.tsx:199-203` fetches `/listings/stats`, `/listings/my` and `/dashboard/seller` together. A code comment at `:37-44` records that `/dashboard/seller` does not return `soldListings`/`totalRevenue`, so mobile deliberately sources those from `/listings/stats` — a documented, correct workaround for a backend shortfall. |
| DASH-009 | DASH | Seller | Earnings + CSV export | `src/app/dashboard/user/page.tsx:1703-1711` (`EarningsTab`) | `GET /listings/earnings`, `/listings/earnings/export` (`listings.controller.ts:246-296`) | PRESENT | — | — | — | Both export CSV. Mobile `EarningsScreen.tsx:115-149` uses a raw `fetch` (not `apiClient`) because the response is CSV not JSON, then writes via `FileSystem` and shares via `expo-sharing`. Same pattern on `DealerEarningsScreen.tsx:113-147`. |
| DASH-010 | DASH | Seller | Auction bonuses in earnings | `src/app/dashboard/seller/earnings/page.tsx:300-377` (orphaned) | `sellerBonus`, `auctionSales` | MISSING | data | M | P2 | Web's orphaned earnings page has a dedicated "Auction Sales" tab showing winning bidder, winning bid and the **£100 seller bonus** per auction. A grep for `sellerBonus|auctionSales` across mobile's `EarningsScreen.tsx` and `DealerEarningsScreen.tsx` returns **zero matches** — a mobile seller cannot see what auction bonuses they have earned anywhere. Note web's unified `EarningsTab` also lacks this, so it is only on the orphaned route. |
| DASH-011 | DASH | Seller | Receipts tab | `src/app/dashboard/seller/earnings/page.tsx:154-164` (orphaned); `src/app/dashboard/dealer/earnings/page.tsx:157` | — | PARTIAL | functionality | S | P3 | Web renders a `ReceiptsTab` component for platform fees, listing charges and KYC payments. Mobile links to a separate `PaymentHistory` screen instead (`EarningsScreen.tsx:298-314`, `DealerEarningsScreen.tsx:275-288`) fed by `GET /payments/history`. Different data source than web's receipts tab — I did not read `ReceiptsTab.tsx`, so I cannot confirm the two show the same rows. |
| DASH-012 | DASH | Seller | Performance metrics | `src/app/dashboard/user/page.tsx:1533-1593` (`StatsTab`) | `GET /listings/performance` | DIVERGENT | data | S | P2 | Three different metric sets exist for one concept. Web's unified `StatsTab` shows Conversion Rate, Active Listings, Avg Response Time, Seller Rating. Web's orphaned `/dashboard/seller/performance:44-49` shows Total Revenue, Profile Views, Total Listings, Conversion Rate. Mobile `SellerPerformanceScreen.tsx:180-209` shows TOTAL REVENUE, TOTAL VIEWS, LISTINGS, CONVERSION — matching the orphaned page, not the live one. Mobile adds trend charts web has not got; mobile omits Avg Response Time and Seller Rating. |
| DASH-013 | DASH | Seller | Offers — respond actions | `src/app/dashboard/user/page.tsx:899-1128` (`OffersTab`) | `PATCH /offers/:id/respond` | PRESENT | — | — | — | Accept / Decline / Counter on both. Mobile `SellerOffersScreen.tsx:229-287`. Mobile refetches on every screen focus (`:197-207`) where web polls every 30s — both reasonable for their platform. |
| DASH-014 | DASH | Seller | Seller counter-attempt lock | `src/app/dashboard/seller/offers/page.tsx:221-222,284-290` (orphaned) | 5-attempt cap (`backend/src/offers/offers.service.ts:294-296`) | PRESENT | — | — | — | Mobile correctly locks the Counter button once `counterAttemptsSeller >= 5` (`SellerOffersScreen.tsx:423,529-538`), mirroring the backend. On web this lock is implemented on the **orphaned** offers page; whether the unified `OffersTab` enforces it was not confirmed. Mobile matches the better web implementation. |
| DASH-015 | DASH | Seller | Mark as sold | `src/app/dashboard/seller/offers/page.tsx:182` (orphaned) | `PATCH /listings/:id/sold` (`RecordSaleDto`) | PRESENT | — | — | — | Mobile `SellerOffersScreen.tsx:289-373` captures `soldPrice`, `buyerId`, and optional `buyerName`/`buyerEmail` — the fullest use of `RecordSaleDto` anywhere in either app. Compare SELL-024, where web's dealer inventory uses the generic status endpoint and captures no sale price at all. |
| DASH-016 | DASH | Seller | Cancel and relist after accepting | `src/app/dashboard/seller/offers/page.tsx:442-455` (orphaned) | `PATCH /offers/:id/respond {REJECTED}` | PRESENT | — | — | — | Backend explicitly permits ACCEPTED → REJECTED, reverting the listing to ACTIVE (`offers.service.ts:285,397-404`). Mobile implements it (`SellerOffersScreen.tsx:289-373`). On web it exists only on the orphaned page, not the unified Offers tab. |
| DASH-017 | DASH | Seller | Delivery requests inside offers | `src/app/dashboard/seller/offers/page.tsx:628-650,785-800` (orphaned) | `/delivery-requests/*` (`delivery.controller.ts:27-171`) | PRESENT | — | — | — | Mobile renders `ReceivedDeliveryRequestsPanel` as a list header on both `SellerOffersScreen.tsx:668` and `DealerOffersScreen.tsx:638`. Again present on web only at the orphaned route. |
| DASH-018 | DASH | Notifications | Notification list and mark-read | n/a (web bell component not read) | `GET /notifications`, `PATCH /notifications/:id/read`, `/read-all` (`notifications.controller.ts:36-73`) | PRESENT | — | — | — | Mobile `NotificationsScreen.tsx:83,99-107,229-241` — grouped by date, optimistic mark-read, mark-all. Backend note: `Notification.type` is a plain `String`, not a Prisma enum (`schema.prisma:1072`), so the type set is open-ended. |
| DASH-019 | DASH | Notifications | Notification list does not update live | n/a | `notification:new` on `/notifications` namespace (`notifications.gateway.ts:76-108`) | PARTIAL | state | S | P2 | Mobile's real-time notification handling lives entirely in `GlobalToastProvider.tsx:80,107-108`, which connects to `/notifications` and raises a toast. `NotificationsScreen.tsx` has **no socket at all** (grep for `io(` returns 0). So a notification arriving while the list is open shows a toast but never appears in the list until manual pull-to-refresh. Also note `GlobalToastProvider.tsx:107-108` listens for **both** `notification:new` and `notification`; the gateway only ever emits `notification:new` (`notifications.gateway.ts:108`), so the second listener is dead. |
| DASH-020 | DASH | Notifications | Deep-linking from a notification | n/a | `entityType`/`entityId`/`link` on the notification row | PRESENT | — | — | — | Mobile has a thorough, role-aware router: `NotificationsScreen.tsx:99-227` handles auction entities, `MESSAGE_RECEIVED` with `data.roomId`, and ~20 type cases branching on role and on whether `link` contains `/buyer/`. This is more complete than the `NOTIFICATION_SCREEN_MAP` fallback in `App.tsx:37-48` noted in Pass 1. |
| DASH-021 | DASH | Notifications | Notification preferences | n/a (web has no equivalent found) | `preferences.notifications` on the user, honoured at `notifications.service.ts:87-140` | PRESENT | — | — | — | Reverse gap — **mobile only**. `NotificationSettingsScreen.tsx:66-113` offers per-type toggles, mute-all, push/email channels, alert frequency and quiet hours, saved via `PATCH /users/me`. The backend genuinely honours these (mute-all, per-type key, quiet hours) before emitting or pushing. Web's dealer settings has four notification checkboxes with **no save handler at all** (`dashboard/dealer/settings/page.tsx:314-337`) and buyer settings likewise uses uncontrolled `defaultChecked` boxes never included in the save payload (`buyer/settings/page.tsx:205-223`). Mobile is the only working implementation. **DECIDED:** mobile is correct; web’s toggles are a bug that saves nothing. This is a WEB defect, not a mobile-ahead row (OQ-24). |
| DASH-022 | DASH | Messaging | Chat real-time | `src/components/dashboard/ChatWindow.tsx:84,92-160` | `/chat` namespace (`chat.gateway.ts:26-323`) | PRESENT | — | — | — | Both connect to `/chat` and handle `message:new`, `user:typing`, `messages:read`, `error`. Mobile `context/ChatContext.tsx:67-113,174-194` emits `message:send`, `typing:start`/`stop`, `message:read`. Genuine parity on the transport. |
| DASH-023 | DASH | Messaging | Presence (online/offline) | `src/components/dashboard/ChatWindow.tsx:92-160` (`presence:snapshot`, `presence:update`) | Gateway emits both (`chat.gateway.ts:110-124,162-166`) | MISSING | functionality | S | P2 | The backend broadcasts `presence:update` and sends a `presence:snapshot` of which conversation partners are online. Web consumes both and exposes `onlineUserIds` via `ChatContext`. Mobile's `ChatContext.tsx:93-113` listens only for `message:new`, `user:typing`, `messages:read` and `error` — **neither presence event is handled**, so mobile never shows who is online. |
| DASH-024 | DASH | Messaging | Contact support | `src/components/dashboard/DashboardSidebar.tsx:97-110,329-338` | `POST /chat/support` (`chat.controller.ts:80-88`) | MISSING | functionality | S | P2 | Web has a "Contact Support" button in the sidebar for every role, creating a room with the oldest ADMIN user. A grep for `chat/support` across mobile `src/` returns **zero matches** — mobile users have no in-app route to support. |
| DASH-025 | DASH | Messaging | Room list and unread | `src/app/dashboard/user/page.tsx:1599-1666` | `GET /chat/rooms`, `/chat/unread` | PRESENT | — | — | — | Mobile `MessagesScreen.tsx:175-217` with All/Offers/Archived tabs and search. Note the Archived tab is hardcoded empty with an inline comment saying archive is not supported on mobile initially — a deliberate placeholder, not a bug, but it shows an empty tab to users. |
| DASH-026 | DASH | Settings | Profile settings | `src/app/dashboard/user/page.tsx:1978-2062` (`SettingsTab`) | `PATCH /users/me` | PRESENT | — | — | — | Mobile `SettingsScreen.tsx:277-301,412-517` covers avatar upload, names, phone, and two notification toggles. Web additionally requires location and postcode with blocking validation (`user/page.tsx:1883-1898`); mobile does not enforce those here (they are collected in onboarding — AUTH-023). |
| DASH-027 | DASH | Settings | Password change | `src/app/dashboard/user/page.tsx:2064-2122` | `POST /auth/reset-password` (`ResetPasswordDto`, MinLength 8) | PRESENT | — | — | — | Mobile `SettingsScreen.tsx:304-332,694-756` validates required fields, match, and min 8 characters — correctly matching the backend DTO, unlike web's public reset page which uses 6 (see AUTH-008 / OQ-2). |
| DASH-028 | DASH | Settings | Stripe Connect payouts | `src/app/dashboard/user/page.tsx:2124-2165` | `GET /users/stripe-connect/status`, `POST /users/stripe-connect/onboard` | PARTIAL | state | S | P2 | Mobile `SettingsScreen.tsx:269-274,758-800` fetches status and shows connected / partial / not-connected states — good parity. But `EarningsScreen.tsx:151-162,329-358` has a **second, inconsistent** entry point to the same onboarding: it sends no `returnUrl`/`refreshUrl` body and never fetches status, so it always shows a bare "Set up bank payouts" CTA even for a seller who is already connected. Two entry points, one of them misleading. |
| DASH-029 | DASH | Settings | Bank account details fallback | `src/app/dashboard/user/page.tsx:2167-2220` | `PATCH /users/me/bank-details` | PRESENT | — | — | — | Mobile `SettingsScreen.tsx:360-382,802-857` — account holder name, sort code, account number, sets `payoutPreference:'BANK'`. |
| DASH-030 | DASH | Settings | Account deletion | `src/app/dashboard/user/page.tsx:2228` (`DeleteAccountSection`) | `DELETE /users/me` (`users.controller.ts:231-262`) | MISSING | functionality | M | P1 | Re-confirms AUTH-033 from a second angle. Web puts `DeleteAccountSection` in the unified Settings tab, the buyer settings page and the dealer settings page. A grep across mobile `src/` for `DELETE /users/me` or `deleteAccount` returns **zero matches**. App-store policy generally requires in-app deletion for apps that allow account creation. |
| DASH-031 | DASH | Settings | Trader address verification | *(no web equivalent found)* | `POST /users/me/address-verification/{start,confirm}` (`users.controller.ts:110-139`) | PRESENT | — | — | — | Reverse gap — **mobile only**. `SettingsScreen.tsx:226-263,664-692` implements the two-step address then 6-digit-code flow against endpoints that exist in the backend but which I found no web UI for. |
| DASH-032 | DASH | Dealer | Dealer verification gate | `src/app/dashboard/dealer/layout.tsx:144-218` | `dealerProfile.isVerified` or staff membership | PRESENT | — | — | — | Both gate on `isVerified || isDealerStaff` and both show a locked feature grid with the same six labels. Mobile `components/DealerGate.tsx:51-102`, applied via `withDealerGate` to nine screens (`MainStackNavigator.tsx:66-74`), with `DealerKYC` and `DealerOnboarding` correctly left ungated. Staff are treated as verified on both sides. |
| DASH-033 | DASH | Dealer | Dealer phone requirement | `src/app/dashboard/dealer/layout.tsx:15-89,220-224` | `PATCH /users/dealer-profile` | DIVERGENT | functionality | S | P3 | Web hard-blocks the entire dealer dashboard behind a full-screen `DealerPhoneGate` modal until a dealer phone is saved, exempting staff. Mobile shows a dismissible-but-recurring banner instead (`DealerProfileScreen.tsx:96-108,615-634`). Deliberate softening — flagging so the divergence is a decision, not a drift. |
| DASH-034 | DASH | Dealer | Dealer navigation | `src/config/dealerRouteConfig.ts:26-125` via `DashboardSidebar.tsx:173-178` | — | PRESENT | — | — | — | Web has 14 dealer nav entries. Mobile's `GlobalDrawer.tsx:160-274` has 15 covering the same ground plus onboarding/KYC. Mobile maps Wishlist to the `Watchlist` screen, which BUY-021 flagged as unreachable dead code from every other entry point — so the dealer drawer is the **only** live route into it. |
| DASH-035 | DASH | Dealer | Dealer home | `src/app/dashboard/dealer/page.tsx:218-263` | `GET /dashboard/dealer`, `GET /dealers/leads` | PARTIAL | data | M | P2 | Web's four KPI tiles are Active Stock, Total Views, Active Leads, Vehicles Sold, plus a Recent Leads table. Mobile `DealerProfileScreen.tsx:169-537` is structured quite differently: TODAY / THIS WEEK sub-tabs, an inventory snapshot bar, top listings, and a "Needs attention" action list, sourced from `GET /dealers/stats` + `GET /dealers/analytics?range=7d`. Richer in places but the four headline KPIs are not directly comparable — reconcile the tile set. |
| DASH-036 | DASH | Dealer | Dealer analytics | `src/app/dashboard/dealer/analytics/page.tsx:133-302` | `GET /dealers/analytics?range=` | PARTIAL | functionality | M | P2 | Web shows 12 KPIs across four rows plus 10 charts, including `SalespersonTable`, `CustomerAreaTable` (by postcode), `LeadSourceChart` and `SalesByTypeChart`. Mobile `DealerAnalyticsScreen.tsx:402-706` covers revenue, units, sell time, views, listings, lead funnel, inventory aging and top performers, plus a conversion deep-dive web lacks. **Missing on mobile:** salesperson performance, customer-area/postcode breakdown, lead-source breakdown, auction-vs-classified split. Web also has a working CSV export (`analytics/page.tsx:96-126`); mobile has none. |
| DASH-037 | DASH | Dealer | Lead pipeline / CRM | `src/app/dashboard/dealer/crm/page.tsx:19-26,255-305` | `GET/POST /dealers/leads`, `PATCH /dealers/leads/:id` | PRESENT | — | — | — | Answers the open question directly: **`DealerLeadsScreen.tsx` is mobile's CRM.** Its own comment at `:75-79` says the board mirrors web's CRM stage set as a tap-based board rather than drag-and-drop. Same six stages, same endpoints, same create/assign/notes/status actions, plus a List view web lacks. The only real difference is drag-and-drop versus a tap stage-picker — an appropriate native adaptation. |
| DASH-038 | DASH | Dealer | Offers received vs offers made | `src/app/dashboard/dealer/offers/page.tsx:38`; `my-offers/page.tsx:16,41` | `GET /offers/received`; `GET /offers/my` | PRESENT | — | — | — | The split is identical on both. Mobile `DealerOffersScreen.tsx:171-172` (received) and `DealerMyOffersScreen.tsx:141` (made). Mobile's received screen additionally gates "Mark as Sold" on `listing.status !== 'SOLD'`, which web does not — a documented mobile-ahead deviation. |
| DASH-039 | DASH | Dealer | Purchases pipeline | `src/app/dashboard/dealer/purchases/page.tsx:25-30,99-121` | `GET /dealers/purchases` | PARTIAL | functionality | S | P3 | Same four-stage pipeline and endpoint on both. Mobile `DealerPurchasesScreen.tsx:77-109,129` shows a totals bar and a card list; web additionally offers a Kanban view toggle alongside the list. |
| DASH-040 | DASH | Dealer | Team management | `src/app/dashboard/dealer/team/page.tsx:43-74` | `GET/POST /dealers/staff`, `DELETE /dealers/staff/:id` | PRESENT | — | — | — | Identical: same three roles (ADMIN, SALES_AGENT, FINANCE_MANAGER), same invite-by-email flow, same pending-invitations section, same destructive-only remove. Mobile `DealerTeamScreen.tsx:222-296`. **Neither app enforces per-role permissions** — see OQ-23. |
| DASH-041 | DASH | Dealer | KYC submission | `src/components/dashboard/KycOverlayForm.tsx` (not read) | `GET/POST /dealers/kyc`, `POST /dealers/kyc/checkout` | PRESENT | — | — | — | Mobile `DealerKYCScreen.tsx:204-432` is thorough: company/representative/director fields, three document uploads to `kyc/{userId}/…`, the £1 Stripe checkout in a WebView, a resume-payment path when saved-but-unpaid, and a 5×1.5s poll for `stripeChargedAt`. I did not read web's `KycOverlayForm`, so I am not asserting which is richer — only that mobile's is complete and traced. |
| DASH-042 | DASH | Dealer | Dealer payouts and bank details | *(absent on web)* | `GET /users/stripe-connect/status`, `PATCH /users/me/bank-details` | PRESENT | — | — | — | Reverse gap — **mobile is ahead**. A grep of `src/app/dashboard/dealer/settings/page.tsx` for `bankAccount|stripe|payout` returns **0 matches**, and the web dealer earnings page likewise has none. Mobile dealers reach the shared `SettingsScreen` payouts and bank-details sections (DASH-028/029). So on web a dealer has no way to configure where the £100 auction bonus is paid — compare AUC-025, which found the reverse for the seller-side warning. |
| DASH-043 | DASH | Dealer | Finance applications | `src/app/dashboard/dealer/finance/page.tsx:36,180-203` | `GET /finance/my`, `PATCH /finance/:id/status` | DIVERGENT | functionality | S | P3 | Deliberate divergence, documented in mobile's own comment (`DealerFinanceScreen.tsx:34-40`): web renders Approve/Review/Reject buttons referencing `FUNDED`/`REVIEWING` statuses that do not exist in the schema, and its PATCH 403s for anyone without a `FINANCE_PARTNER` profile. Mobile shows the dealer's own applications read-only instead of porting dead buttons. Mobile is right here; the web page is the one to fix. |
| DASH-044 | DASH | Dealer | Dealer earnings "Financial Report" button | `src/app/dashboard/dealer/earnings/page.tsx:88-90` | — | PRESENT | — | — | — | Web-side defect logged so it is not ported: the "Financial Report" button has **no `onClick`** — a dead control. Mobile's equivalent export works (`DealerEarningsScreen.tsx:113-147,222-232`). |
| DASH-045 | DASH | Dealer | Auctions-won summary page | `src/app/dashboard/dealer/auctions/won/page.tsx:398-513` | `getWonAuctions`, `getMyBids` | PARTIAL | functionality | M | P2 | Web aggregates live bids and every won auction into one prioritised view: "Needs your attention" (fee due), "Auctions you're bidding on" with live countdowns, "Handover in progress", "Completed". Mobile splits this across `BuyerBidsScreen` and `SellerAuctionsScreen`'s WON tab (AUC-027) with no equivalent prioritised "needs attention" grouping. Note web's `denied` stage is unreachable — `stageFor()` never returns it (`won/page.tsx:46-50` vs `:400`). |
| DASH-046 | DASH | Roles | Partner and admin dashboards | `src/app/dashboard/{finance,insurance,service,admin}/**` | `/dashboard/{contractor,finance,insurance,admin}` (`dashboard.controller.ts:59-91`) | MISSING | permissions | L | P3 | Web has full dashboards for FINANCE_PARTNER, INSURANCE_PARTNER, CONTRACTOR and ADMIN (15 admin pages alone), each backed by its own endpoint. Mobile has none and folds these roles into `buyer` (`store/authStore.ts:117`). Duplicate of AUTH-027/AUTH-029 seen from the dashboard side — **do not scope until OQ-5 is answered**; it may be intentionally out of scope for mobile. **DECIDED: OUT OF SCOPE.** Partner/admin dashboards are not coming to mobile (OQ-5). |
| DASH-047 | DASH | Cross | Silent error handling on dashboards | n/a | — | DIVERGENT | state | M | P2 | Mobile dashboards almost universally swallow fetch failures — `catch { /* show zeros */ }` — so a failed request renders as legitimate-looking zeros rather than an error. Only `BuyerPurchaseHistoryScreen` and `EarningsScreen`'s Stripe flow surface an `ErrorBanner`. A dealer whose analytics call fails sees a dashboard reporting zero sales. Overlaps Pass 6 (CROSS) — recorded here because it was observed across every screen in this pass. |
| CROSS-001 | CROSS | Design | Brand red — three-way divergence | `src/app/globals.css:6` — `#ed1c24` | Design system: `#ff0037` (`CarMazium Design System/colors_and_type.css:13`) | PRESENT | — | — | — | **Mobile is correct; web is stale.** The design system's `SKILL.md:47` states as a hard rule: "The primary brand red is `#FF0037`, not the old `#ED1C24`". Mobile uses `#FF0037` (`src/constants/colors.ts:39`). Web still uses `#ed1c24` (`globals.css:6`), so the entire web app renders the superseded brand red. Not a mobile gap — recorded so nobody "fixes" mobile toward web. Note the design-system file is itself mid-migration: its own `--gradient-vip-tab` (`colors_and_type.css:164`) and shadow tokens (`:77-79`) still contain `#ed1c24`. See OQ-25. **DECIDED:** `#FF0037` canonical, web is stale. No mobile change — recorded so mobile is never "fixed" toward web’s old red (OQ-25). |
| CROSS-002 | CROSS | Design | Secondary brand colour | `src/app/globals.css:7` — `#1e293b` | Design system `--cz-secondary: #2d3c63` (`colors_and_type.css:14`) | PRESENT | — | — | — | Same shape as CROSS-001. Mobile `brandSlate = '#2D3C63'` (`colors.ts:45`) matches the design system exactly. Web uses `#1e293b`, which is Tailwind slate-800, not the brand navy. Mobile correct, web divergent. |
| CROSS-003 | CROSS | Design | Semantic accent colours | *(not defined as tokens on web)* | `--cz-amber/blue/emerald/indigo` (`colors_and_type.css:26-32`) | PRESENT | — | — | — | Mobile defines and matches all of them: `success #10B981`, `successLight #34D399`, `warning #F59E0B`, `warningLight #FBBF24`, `info #3B82F6`, `infoLight #60A5FA` (`colors.ts:49-53`). Web defines **no** accent tokens in `globals.css` at all (grep returned none) and presumably uses raw Tailwind utilities. Mobile has the better token layer. Only gap: mobile has no dedicated `indigo` token, folding it onto `info`. |
| CROSS-004 | CROSS | Design | Hardcoded colours in mobile source | n/a | `src/constants/colors.ts` | PRESENT | — | — | — | Mobile token discipline is excellent and better than expected: a grep for `#RRGGBB` across `src/screens/` returns **0 matches**, and across `src/components/` only `GradeChip.tsx` has any (5). A prior codemod (documented in `colors.ts:24-27`) evidently succeeded. |
| CROSS-005 | CROSS | Design | Hardcoded typography in mobile | n/a | `src/constants/typography.ts` | PRESENT | — | — | — | Equally clean: **0** inline `fontSize:` numeric literals and **0** inline `fontFamily:` string literals across `src/screens/`. Everything routes through `FontFamily`/`FontSize`/`Type`. |
| CROSS-006 | CROSS | Design | Deprecated token aliases still in use | n/a | — | PARTIAL | visual | M | P3 | Mobile's own files flag unfinished migration: `colors.ts:930-1046` is a "DEPRECATED ALIASES" block, and `typography.ts` documents that call sites still route through alias names (`size12` used ~171×, `size9` ~142×, `size14` ~131×) rather than the canonical `Type.*` scale, with a comment saying migration happens "during the Phase 2 sweep". Cosmetically harmless today; it means the token layer has two names for many values. |
| CROSS-007 | CROSS | Design | Type scale differs from the design system | n/a | `colors_and_type.css:120-148` | DIVERGENT | visual | M | P3 | Mobile's `Type` scale (`typography.ts:53-74`) uses different absolute sizes than the design system's CSS scale — mobile h1 **32px** vs design-system **36px**; mobile body **14px** vs design-system **16px**. Defensible for native (smaller viewports, different reading distance), but it is an undocumented divergence. Also `typography.ts:192` hardcodes eyebrow tracking as `1.8` with a comment calling it "≈0.18em", where the design system specifies `0.15em` (`colors_and_type.css:144`). |
| CROSS-008 | CROSS | Design | Body font conflict on web | `src/app/layout.tsx:97` (`inter.className`) vs `globals.css:9,103` (`--font-sans: Montserrat`) | Design system: Montserrat body, Inter "alongside" | PRESENT | — | — | — | Web-side inconsistency, logged so mobile does not copy it. `globals.css` sets `body { font-family: var(--font-sans) }` → Montserrat, but `layout.tsx:97` applies `inter.className` directly to `<body>`. The design system's own README acknowledges this ("Inter sneaks in as the default body font"). Mobile is unambiguous: Montserrat for body/UI, Poppins for headings, JetBrains Mono for numerics (`typography.ts:15-27`). Mobile installs Inter but never exposes it as a token — harmless. |
| CROSS-009 | CROSS | Errors | Error surfacing is inconsistent on mobile | n/a | — | DIVERGENT | state | M | P2 | Mobile has a shared `ErrorBanner` (`src/components/ui/ErrorBanner.tsx`) used in 18 screens, but `Alert.alert` appears at **173 call sites across 26 screens**, and **13 screens use both** with no discernible rule. Worst by volume: `SellCarFlowScreen` (28), `SettingsScreen` (18), `AuctionDetailScreen` (14), `SellerAuctionsScreen` (14), `SellerListingsScreen` (13). Good news: a grep for bare `catch {}` in `src/screens` returns **0** — errors are caught, just surfaced inconsistently. |
| CROSS-010 | CROSS | Errors | Web uses native browser dialogs | 74 `alert(` call sites across 23 files; `window.confirm` in 9 files | — | PRESENT | — | — | — | Web-side finding, not a mobile gap — recorded because it is the single largest UX inconsistency found on web. There is no toast library installed (no `sonner`, no `react-hot-toast`); seven pages hand-roll local toast state. Destructive confirmations and errors go through native `alert()`/`confirm()`. Mobile's `Alert.alert` is at least the platform-native convention; web's is not. |
| CROSS-011 | CROSS | Errors | App-level error boundary | `src/app/error.tsx`, `src/app/global-error.tsx` | — | NEEDS_VERIFICATION | state | M | P2 | Web has two route-level error boundaries that render a branded recovery card with "Try again"/"Go home". I did **not** check whether mobile has a React error boundary at the root — `App.tsx` was read in Pass 1 and I do not recall one, but I did not grep for `componentDidCatch`/`ErrorBoundary` in this pass. Do not treat as a gap until verified. Note neither app reports to a monitoring service — both only `console.error`. |
| CROSS-012 | CROSS | Loading | Skeletons vs spinners | 100 files use `Loader2`; only 2 use a skeleton pattern | — | PRESENT | — | — | — | Reverse gap — **mobile is ahead**. Mobile has a shared `Skeleton` component used in 29 screens; web has no shared skeleton and uses the `Loader2` spinner in 100 files. Mobile's gap is narrower: 19 screens use only `ActivityIndicator` with no skeleton, including high-traffic `VehicleDetailScreen` and `SellerProfileScreen`. |
| CROSS-013 | CROSS | Loading | Route-level loading UI | 7 `loading.tsx` files | — | PRESENT | — | — | — | Web has `loading.tsx` for 7 of 98 pages — all detail/search routes. **No** dashboard sub-route has one (~30+ pages). Not applicable to mobile in the same form; recorded for completeness so the comparison is not mistaken for a mobile gap. |
| CROSS-014 | CROSS | Empty | Shared empty-state component | *(none exists on web)* | — | PRESENT | — | — | — | Reverse gap — **mobile is ahead**. Mobile has a shared `EmptyState` (`src/components/ui/EmptyState.tsx`) used in 26 screens. A grep for any `EmptyState` component in web `src/components` returns **0** — every web empty state is bespoke inline JSX. |
| CROSS-015 | CROSS | Offline | Network / offline detection | *(none)* | — | MISSING | functionality | M | P1 | **Neither app has any.** No `@react-native-community/netinfo` in mobile's `package.json` and no usage in `src/` (both greps return nothing); no `navigator.onLine` and no service worker on web. This is shared absence, but it matters far more on mobile: `src/lib/apiClient.ts` has no reachability branch, so an offline request rejects with a raw RN `TypeError: Network request failed` that is neither the `NO_SESSION`, `REQUEST_TIMEOUT` nor `AUTH_REDIRECT` sentinel, and each screen renders it differently. There is no "you're offline" banner and no retry-on-reconnect anywhere. |
| CROSS-016 | CROSS | Offline | Repo-root `sw.json` is not a service worker | n/a | — | PRESENT | — | — | — | Clarification so it is not mistaken for offline support: `sw.json` at the repo root is an **OpenAPI/Swagger spec** (`{"openapi":"3.0.0",...}`), misleadingly named, and referenced nowhere in `src/` or `public/`. It is also untracked in git. |
| CROSS-017 | CROSS | Network | Retry policy | `src/lib/fetchWithRetry.ts` used at 2 call sites; `apiClient` at 38 | — | DIVERGENT | state | S | P2 | Web has a retry wrapper (60s timeout, 2 retries, built for Render/Fly cold starts) but uses it **only** in the auth callback and signup flows. Every other web flow gets a single-attempt 30s call. Mobile has **no retry wrapper at all** and a shorter 10s timeout (`src/lib/apiClient.ts:369`) — so mobile is more exposed to backend cold starts than web on every flow, and equally exposed on the auth flow web specifically hardened. |
| CROSS-018 | CROSS | Lists | Pagination — silent truncation | 10 web pages fetch a fixed limit and never paginate | Backend `limit` has `@Min(1)` and **no `@Max`** (`listing-filter.dto.ts:209-214`) | PARTIAL | data | M | P1 | Both apps truncate silently, mobile slightly worse. On mobile, `onEndReached` appears in exactly **one** screen (`SearchScreen.tsx`); everything else fetches page 1 at a fixed limit: `DealerInventoryScreen.tsx:572` (50), `DealerLeadsScreen.tsx:487` (50), `DealerPurchasesScreen.tsx:129` (100), `SellerListingsScreen.tsx:215` (50), `SellerAuctionsScreen.tsx:217,238` (50), `MyListingDashboardScreen.tsx:125` (20), `CompareScreen.tsx:124` (30). A dealer with more than 50 leads or listings simply never sees the rest, with no indication anything is missing. Web has the same problem on 10 pages. Compare BUY-020, where the watchlist store caps at 50. |
| CROSS-019 | CROSS | Lists | Pull-to-refresh coverage | n/a (not a web pattern) | — | PARTIAL | functionality | S | P3 | Mobile uses `RefreshControl` in 27 screens. List screens missing it: `PaymentHistoryScreen`, `DealerMyOffersScreen`, `MyListingDashboardScreen` (`ChatScreen` and `OnboardingScreen` also lack it but are not data lists, so are reasonable exclusions). |
| CROSS-020 | CROSS | Feedback | Toast usage | 7 web pages hand-roll local toast state; no library | `GlobalToastProvider` | PARTIAL | visual | S | P3 | Mobile has a proper shared toast system (`components/GlobalToastProvider.tsx`, `components/Toast.tsx`, fully tokenised) — but it is used directly in only **4 screens / 13 call sites**, mostly watchlist saves and socket notifications. General success and error feedback goes through `Alert.alert` instead (CROSS-009). The system is good; adoption is thin. |
| CROSS-021 | CROSS | Nav | Unreachable mobile screen | n/a | — | DIVERGENT | navigation | S | P2 | `BuyerDashboardScreen.tsx` (624 lines) is **dead code**. It is imported only by `MainStackNavigator.tsx:40,286`; there are zero `navigate('BuyerDashboard')` call sites, and it is not among the 33 `stackScreen` targets in `GlobalDrawer.tsx`. Compare `UnifiedDashboardScreen`, whose stack route is likewise never navigated to but whose component **is** live because `TabNavigator.tsx:29` renders it directly. Second confirmed dead screen after `WatchlistScreen` (BUY-021). **This amends DASH-004** — see the note on that row. |
| CROSS-022 | CROSS | Nav | Orphaned web routes beyond the dashboards | `src/app/auth/partners/page.tsx` | — | PRESENT | — | — | — | Web-side finding. `/auth/partners` has **zero** inbound references anywhere in `src/` — which corroborates OQ-7, where I found its `POST /auth/login` call contradicts the backend DTO. It appears to be both broken and unreachable. `/checkout/{success,cancel}` also have no in-app links but are reached via Stripe redirect URLs, so they are not true orphans. |
| CROSS-023 | CROSS | Errors | Failures render as legitimate-looking zeros | n/a | — | DIVERGENT | state | M | P2 | Carried forward from DASH-047. Mobile dashboards overwhelmingly `catch { /* show zeros */ }`, so a failed stats request is indistinguishable from a genuine zero. A dealer whose `/dealers/analytics` call fails sees a dashboard reporting zero sales, zero leads and zero revenue with no error indicator. Only `BuyerPurchaseHistoryScreen` and `EarningsScreen`'s Stripe flow surface an `ErrorBanner`. This is the highest-value cross-cutting fix on mobile. |
| CROSS-024 | CROSS | A11y | Accessible labels | 37 `aria-label` across ~494 `<button>` elements | — | NEEDS_VERIFICATION | visual | M | P3 | Rough web signal only — many buttons have visible text that satisfies an accessible name, so the ratio overstates the issue. I did **not** audit mobile `accessibilityLabel` coverage at all in this pass. Recorded as a known-unaudited area rather than a finding; do not treat either number as a verified gap. |
