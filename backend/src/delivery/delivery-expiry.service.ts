/**
 * DeliveryExpiryService — skeleton stub for TDD RED state (Phase 15 Plan 01).
 * Real implementation (cron sweep) in Plan 02.
 */
export class DeliveryExpiryService {
  async handleDeliveryExpiry(): Promise<void> {
    throw new Error('not implemented');
  }
}
